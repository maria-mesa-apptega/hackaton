generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model Agio_Update {
  Action                      String? @db.Text
  questionid                  String? @db.Text
  Agio_assessment_template_id String? @db.Text
  current_question            String? @db.Text
  Agio_ID                     String? @db.Text
  updated_question            String? @db.Text

  @@map("Agio Update")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model CMMCLevel1Assessment {
  Information_to_Include_in_Question String? @map("Information to Include in Question") @db.Text
  Question                           String? @db.Text
  Example                            String? @db.Text
  Category                           String? @db.Text
  Subcategory                        String? @db.Text
  Question_Type                      String? @map("Question Type") @db.Text
  mapping                            String? @db.Text
  sub_control_template_id            Int?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model CMMCLevel2Assessment {
  Question                String? @db.Text
  Example                 String? @db.Text
  Category                String? @db.Text
  Subcategory             String? @db.Text
  Question_Type           String? @map("Question Type") @db.Text
  mapping                 String? @db.Text
  sub_control_template_id Int?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model CMMCLevel3Assessment {
  Information_to_Include_in_Question String? @map("Information to Include in Question") @db.Text
  Question                           String? @db.Text
  Example                            String? @db.Text
  Category                           String? @db.Text
  Subcategory                        String? @db.Text
  Question_Type                      String? @map("Question Type") @db.Text
  mapping                            String? @db.Text
  sub_control_template_id            Int?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model MasterCXMapping {
  Category_ID Int?
  Sub_ID      Int?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model RL_ca {
  id                      Int    @default(0) @db.UnsignedInt
  name                    String
  description             String @db.Text
  sub_control_template_id Int?

  @@ignore
}

model SequelizeMeta {
  name String @id @unique(map: "name") @db.VarChar(255)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model TEMP_CIS_WIPFLI {
  Tab                           String? @map("ï»¿Tab") @db.Text
  Framework                     String? @db.Text
  App                           String? @db.Text
  Subcontrol                    String? @db.Text
  Subcontrol_Description        String? @map("Subcontrol Description") @db.Text
  Control_Activity_Name         String? @map("Control Activity Name") @db.Text
  Control_Activity_Description  String? @map("Control Activity Description") @db.Text
  Task_Subject                  String? @map("Task Subject") @db.Text
  Assigned_To                   String? @map("Assigned To") @db.Text
  Department                    String? @db.Text
  Recurring                     String? @db.Text
  of_Months_between_Occurrences Int?    @map("#  of Months between Occurrences")
  Date_of_First_Due_Date        String? @map("Date of First Due Date") @db.Text
  of_Occurrences                Int?    @map("# of Occurrences")
  Notes                         String? @db.Text
  task_id                       Int?

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model TEMP_RESTORE {
  id           Int      @default(0) @db.UnsignedInt
  notes        String?  @db.Text
  progress     Int
  app_id       Int      @default(0) @db.UnsignedInt
  app_progress Decimal? @db.Decimal(8, 4)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model Temp_program {
  id              Int?      @default(0) @db.UnsignedInt
  name            String
  organization_id Int       @db.UnsignedInt
  framework_id    Int?      @db.UnsignedInt
  total_budget    Decimal?  @db.Decimal(11, 2)
  start_date      DateTime  @db.Date
  end_date        DateTime  @db.Date
  level           String?
  progress        Decimal?  @db.Decimal(8, 4)
  created_at      DateTime? @db.Timestamp(0)
  updated_at      DateTime? @db.Timestamp(0)
  deleted_at      DateTime? @db.Timestamp(0)

  @@ignore
}

model activities {
  id                      Int                       @id @default(autoincrement()) @db.UnsignedInt
  uuid                    String?                   @unique(map: "activities_uuid_unique") @db.Char(36)
  activity                String
  slug                    String
  created_at              DateTime?                 @db.Timestamp(0)
  updated_at              DateTime?                 @db.Timestamp(0)
  audit_activity_user_map audit_activity_user_map[]
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model added_uuid {
  table String

  @@ignore
}

model admin_users {
  id           Int      @id @default(autoincrement())
  uuid         String?  @unique(map: "admin_users_uuid_unique") @db.Char(36)
  user_id      Int?
  created_at   DateTime @default(now()) @db.DateTime(0)
  updated_at   DateTime @default(now()) @db.DateTime(0)
  uuid_user_id String?  @db.Char(36)
}

model agio_historical_chart_data {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid       String?   @unique(map: "agio_historical_chart_data_uuid_unique") @db.Char(36)
  type       String?
  x_values   Decimal?  @db.Decimal(8, 4)
  y_values   Decimal?  @db.Decimal(8, 4)
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
}

model agio_organization_details {
  id                   BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                 String?   @unique(map: "agio_organization_details_uuid_unique") @db.Char(36)
  organization_id      Int?      @db.UnsignedInt
  users                Int?
  devices              Int?
  networks             Int?
  sites                Int?
  created_at           DateTime? @db.Timestamp(0)
  updated_at           DateTime? @db.Timestamp(0)
  uuid_organization_id String?   @db.Char(36)
}

model api_access_log {
  id           Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid         String?   @unique(map: "api_access_log_uuid_unique") @db.Char(36)
  key_id       Int       @db.UnsignedInt
  source_ip    String
  request_type String
  end_point    String
  created_at   DateTime? @db.Timestamp(0)
  updated_at   DateTime? @db.Timestamp(0)
  deleted_at   DateTime? @db.Timestamp(0)
  uuid_key_id  String?   @db.Char(36)
  api_key      api_key   @relation(fields: [key_id], references: [id], onUpdate: Restrict, map: "api_access_log_key_id_foreign")

  @@index([key_id], map: "api_access_log_key_id_foreign")
}

model api_key {
  id                   Int                   @id @default(autoincrement()) @db.UnsignedInt
  uuid                 String?               @unique(map: "api_key_uuid_unique") @db.Char(36)
  name                 String
  organization_id      Int?                  @db.UnsignedInt
  partner_id           Int?                  @db.UnsignedInt
  created_by           Int                   @db.UnsignedInt
  api_role_id          Int                   @db.UnsignedInt
  status               Boolean
  restrict             Boolean
  api_key              String
  created_at           DateTime?             @db.Timestamp(0)
  updated_at           DateTime?             @db.Timestamp(0)
  deleted_at           DateTime?             @db.Timestamp(0)
  uuid_organization_id String?               @db.Char(36)
  uuid_partner_id      String?               @db.Char(36)
  uuid_created_by      String?               @db.Char(36)
  uuid_api_role_id     String?               @db.Char(36)
  api_access_log       api_access_log[]
  api_role             api_role              @relation(fields: [api_role_id], references: [id], onUpdate: Restrict, map: "api_key_api_role_id_foreign")
  user                 user                  @relation(fields: [created_by], references: [id], onUpdate: Restrict, map: "api_key_created_by_foreign")
  organization         organization?         @relation(fields: [organization_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "api_key_org_id_foreign")
  partner              partner?              @relation(fields: [partner_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "api_key_partner_id_foreign")
  api_key_restriction  api_key_restriction[]

  @@index([api_role_id], map: "api_key_api_role_id_foreign")
  @@index([created_by], map: "api_key_created_by_foreign")
  @@index([organization_id], map: "api_key_org_id_foreign")
  @@index([partner_id], map: "api_key_partner_id_foreign")
}

model api_key_restriction {
  id          Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid        String?   @unique(map: "api_key_restriction_uuid_unique") @db.Char(36)
  key_id      Int       @db.UnsignedInt
  source_ip   String
  description String    @db.Text
  created_at  DateTime? @db.Timestamp(0)
  updated_at  DateTime? @db.Timestamp(0)
  deleted_at  DateTime? @db.Timestamp(0)
  uuid_key_id String?   @db.Char(36)
  api_key     api_key   @relation(fields: [key_id], references: [id], onUpdate: Restrict, map: "api_key_restriction_key_id_foreign")

  @@index([key_id], map: "api_key_restriction_key_id_foreign")
}

model api_role {
  id         Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid       String?   @unique(map: "api_role_uuid_unique") @db.Char(36)
  name       String
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
  deleted_at DateTime? @db.Timestamp(0)
  api_key    api_key[]
}

model app {
  id                   Int              @id @default(autoincrement()) @db.UnsignedInt
  uuid                 String?          @unique(map: "app_uuid_unique") @db.Char(36)
  program_id           Int              @db.UnsignedInt
  app_template_id      Int?             @db.UnsignedInt
  name                 String?          @db.VarChar(500)
  description          String?          @db.VarChar(4056)
  status               Boolean?         @default(true)
  icon                 String?
  s3_bucket            String           @default("AWS_S3_BUCKET_STORAGE")
  s3_driver            String           @default("s3_storage")
  progress             Decimal?         @db.Decimal(8, 4)
  sort_order           Int?
  created_at           DateTime?        @db.Timestamp(0)
  updated_at           DateTime?        @db.Timestamp(0)
  deleted_at           DateTime?        @db.Timestamp(0)
  verified_progress    Decimal?         @default(0.0000) @db.Decimal(8, 4)
  uuid_program_id      String?          @db.Char(36)
  uuid_app_template_id String?          @db.Char(36)
  app_template         app_template?    @relation(fields: [app_template_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "app_app_template_id_foreign")
  program              program          @relation(fields: [program_id], references: [id], onUpdate: Restrict, map: "app_program_id_foreign")
  report_app_map       report_app_map[] @ignore
  sub_control          sub_control[]

  @@index([app_template_id], map: "app_app_template_id_foreign")
  @@index([program_id], map: "app_program_id_foreign")
  @@index([id, status, name, description(length: 191)], map: "app_search")
}

model app_template {
  id                         Int                          @id @default(autoincrement()) @db.UnsignedInt
  uuid                       String?                      @unique(map: "app_template_uuid_unique") @db.Char(36)
  name                       String                       @db.VarChar(500)
  description                String                       @db.VarChar(4056)
  icon                       String
  s3_bucket                  String                       @default("AWS_S3_BUCKET_STORAGE")
  s3_driver                  String                       @default("s3_storage")
  organization_id            Int?                         @db.UnsignedInt
  order                      Int                          @default(0)
  created_at                 DateTime?                    @db.Timestamp(0)
  updated_at                 DateTime?                    @db.Timestamp(0)
  deleted_at                 DateTime?                    @db.Timestamp(0)
  partner_id                 Int?                         @db.UnsignedInt
  uuid_organization_id       String?                      @db.Char(36)
  uuid_partner_id            String?                      @db.Char(36)
  app                        app[]
  organization               organization?                @relation(fields: [organization_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "app_template_organization_id_foreign")
  framework_app_template_map framework_app_template_map[] @ignore
  sub_control_template       sub_control_template[]

  @@index([organization_id], map: "app_template_organization_id_foreign")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model aq {
  s_q_id     Int?
  s_question String?
  d_q_id     Int?
  d_question String?
  s_sc_id    Int?
  d_sc_id    Int?

  @@ignore
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model assessment {
  id                           Int                            @id @default(autoincrement()) @db.UnsignedInt
  uuid                         String                         @db.Char(36)
  version                      String
  name                         String
  user_id                      Int                            @db.UnsignedInt
  org_id                       Int?                           @db.UnsignedInt
  assign                       Boolean?
  is_assigned_user             Int                            @default(0)
  autoscore                    Boolean?
  autotask                     Boolean?
  request_evidence             Boolean?
  is_make_required             Boolean?
  add_recommendation           Int?                           @db.TinyInt
  recommendation               String?                        @db.Text
  is_reccurring                Boolean?
  start_date                   DateTime?                      @db.Date
  end_date                     DateTime?                      @db.Date
  start_time                   String?
  end_time                     String?
  start_timezone               String?
  end_timezone                 String?
  reccur_pattern               String?                        @db.VarChar(20)
  reccur_start_date            DateTime?                      @db.Date
  reccur_end_by                Boolean?
  reccur_end_date              DateTime?                      @db.Date
  reccur_end_after             Boolean?
  reccur_end_after_occurrence  Int?
  reccur_no_end                Boolean?
  reccur_reminder_alert        Boolean?
  description                  String?                        @db.Text
  assessment_template_id       Int?
  copied_from                  Int?
  score                        Decimal                        @default(0.00) @db.Decimal(5, 2)
  framework_id                 Int?                           @db.UnsignedInt
  created_at                   DateTime?                      @db.Timestamp(0)
  updated_at                   DateTime?                      @db.Timestamp(0)
  is_visible                   Boolean                        @default(true)
  deleted_at                   DateTime?                      @db.Timestamp(0)
  is_assign_manager            Int?                           @default(0)
  assign_manager               Int?
  status                       String                         @default("Not Started")
  reviewer_name                String?
  is_add_risk                  Int?                           @default(0) @db.TinyInt
  auto_tasking                 Boolean?
  allow_gpt_recommendation     Boolean                        @default(false)
  gpt_recommendation_config    Json?
  hidden                       Int                            @default(0)
  uuid_user_id                 String?                        @db.Char(36)
  uuid_org_id                  String?                        @db.Char(36)
  uuid_reccur_end_by           String?                        @db.Char(36)
  uuid_assessment_template_id  String?                        @db.Char(36)
  uuid_framework_id            String?                        @db.Char(36)
  checked_scoring              Boolean                        @default(true)
  organization                 organization?                  @relation(fields: [org_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_assessment_organization")
  user                         user                           @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_assessment_user")
  assessment_category_user_map assessment_category_user_map[]
  assessment_occurrences       assessment_occurrences[]
  assessment_question          assessment_question[]
  assessment_question_user_map assessment_question_user_map[]

  @@index([org_id], map: "assessment_org_id_index")
  @@index([uuid], map: "assessment_uuid_index")
  @@index([user_id], map: "fk_assessment_user")
}

model assessment_category {
  id                           Int                            @id @default(autoincrement()) @db.UnsignedInt
  category                     String                         @db.VarChar(200)
  help_text                    String?                        @db.VarChar(500)
  order                        Int?
  created_at                   DateTime?                      @db.Timestamp(0)
  updated_at                   DateTime?                      @db.Timestamp(0)
  uuid                         String?                        @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  assessment_subcategory       assessment_subcategory[]
  assessment_template_question assessment_template_question[]
}

model assessment_category_user_map {
  id                 Int        @id @default(autoincrement()) @db.UnsignedInt
  assessment_id      Int        @db.UnsignedInt
  category_id        Int        @db.UnsignedInt
  user_id            Int?       @db.UnsignedInt
  created_at         DateTime?  @db.Timestamp(0)
  updated_at         DateTime?  @db.Timestamp(0)
  uuid               String?    @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  uuid_assessment_id String?    @db.Char(36)
  uuid_category_id   String?    @db.Char(36)
  uuid_user_id       String?    @db.Char(36)
  assessment         assessment @relation(fields: [assessment_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_assessment_category_user_map_assessment")

  @@index([assessment_id], map: "fk_assessment_category_user_map_assessment")
}

model assessment_occurrences {
  id                 Int        @id @default(autoincrement()) @db.UnsignedInt
  assessment_id      Int        @db.UnsignedInt
  occurrence         Int
  due_date           DateTime?  @db.Date
  status             Boolean
  notified           Int
  created_at         DateTime?  @db.Timestamp(0)
  updated_at         DateTime?  @db.Timestamp(0)
  uuid               String?    @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  uuid_assessment_id String?    @db.Char(36)
  assessment         assessment @relation(fields: [assessment_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_assessment_occurrences_assessment")

  @@index([assessment_id], map: "fk_assessment_occurrences_assessment_idx")
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model assessment_question {
  id                                   Int                            @id @default(autoincrement()) @db.UnsignedInt
  assessment_id                        Int                            @db.UnsignedInt
  assessment_template_question_id      Int?
  question                             String?                        @db.Text
  objective_criteria                   String?
  help_text                            String?                        @db.VarChar(1500)
  category_id                          Int?                           @db.UnsignedInt
  subcategory_id                       Int?                           @db.UnsignedInt
  parent_id                            Int?
  default_response                     String?
  copied_from                          Int?
  autoscore                            Boolean?
  autotask                             Boolean?
  require_note                         Boolean                        @default(false)
  require_notable_practice             Boolean                        @default(false)
  omit_scoring                         Boolean                        @default(false)
  follow_up                            Boolean?
  request_evidence                     Boolean?
  add_recommendation                   Int?                           @db.TinyInt
  recommendation                       String?                        @db.Text
  not_applicable                       Boolean?
  response_type                        String?
  response_json                        String?                        @db.Text
  ssp_report_field                     String?
  created_at                           DateTime?                      @db.Timestamp(0)
  updated_at                           DateTime?                      @db.Timestamp(0)
  deleted_at                           DateTime?                      @db.Timestamp(0)
  scoring_weight                       Int?
  uuid                                 String?                        @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  allow_gpt_recommendation             Boolean                        @default(false)
  gpt_recommendation_config            Json?
  uuid_assessment_id                   String?                        @db.Char(36)
  uuid_assessment_template_question_id String?                        @db.Char(36)
  uuid_category_id                     String?                        @db.Char(36)
  uuid_subcategory_id                  String?                        @db.Char(36)
  uuid_parent_id                       String?                        @db.Char(36)
  assessment                           assessment                     @relation(fields: [assessment_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_assessment_question_assessment")
  assessment_question_user_map         assessment_question_user_map[]

  @@index([assessment_id])
  @@index([ssp_report_field], map: "assessment_question_ssp_report_field_index")
  @@index([uuid_assessment_id], map: "assessment_question_uuid_assessment_id_index")
  @@index([assessment_id], map: "fk_assessment_question_assessment_idx")
  @@index([category_id], map: "fk_assessment_question_category_idx")
}

model assessment_question_risk_map {
  id                          BigInt  @id @default(autoincrement()) @db.UnsignedBigInt
  assessment_id               BigInt  @db.UnsignedBigInt
  risk_id                     BigInt  @db.UnsignedBigInt
  assessment_question_id      BigInt  @db.UnsignedBigInt
  uuid_assessment_id          String? @db.Char(36)
  uuid_risk_id                String? @db.Char(36)
  uuid_assessment_question_id String? @db.Char(36)
}

model assessment_question_subcontrol {
  id                 Int       @id @default(autoincrement()) @db.UnsignedInt
  question_id        Int       @db.UnsignedInt
  subcontrol_id      Int       @db.UnsignedInt
  level              String?
  created_at         DateTime? @db.Timestamp(0)
  updated_at         DateTime? @db.Timestamp(0)
  uuid               String?   @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  uuid_question_id   String?   @db.Char(36)
  uuid_subcontrol_id String?   @db.Char(36)

  @@index([question_id], map: "assessment_question_subcontrol_question_id_index")
  @@index([subcontrol_id], map: "assessment_question_subcontrol_subcontrol_id_index")
  @@index([question_id], map: "fk_assessment_question_idx")
  @@index([subcontrol_id], map: "fk_subcontrol_idx")
}

model assessment_question_template_recommendations {
  id                                   BigInt                                                 @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                 String?                                                @unique(map: "assessment_question_template_recommendations_uuid_unique") @db.Char(36)
  assessment_template_question_id      Int                                                    @db.UnsignedInt
  audience                             assessment_question_template_recommendations_audience  @default(common_language)
  verbosity                            assessment_question_template_recommendations_verbosity @default(brief)
  cached_response                      String                                                 @db.Text
  partner_id                           Int?                                                   @db.UnsignedInt
  organization_id                      Int?                                                   @db.UnsignedInt
  deleted_at                           DateTime?                                              @db.Timestamp(0)
  created_at                           DateTime?                                              @db.Timestamp(0)
  updated_at                           DateTime?                                              @db.Timestamp(0)
  uuid_assessment_template_question_id String?                                                @db.Char(36)
  uuid_partner_id                      String?                                                @db.Char(36)
  uuid_organization_id                 String?                                                @db.Char(36)
}

model assessment_question_user_map {
  id                  Int                 @id @default(autoincrement()) @db.UnsignedInt
  assessment_id       Int                 @db.UnsignedInt
  question_id         Int                 @db.UnsignedInt
  user_id             Int                 @db.UnsignedInt
  created_at          DateTime?           @db.Timestamp(0)
  updated_at          DateTime?           @db.Timestamp(0)
  uuid                String?             @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  uuid_assessment_id  String?             @db.Char(36)
  uuid_question_id    String?             @db.Char(36)
  uuid_user_id        String?             @db.Char(36)
  assessment          assessment          @relation(fields: [assessment_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_assessment_question_user_map_assessment")
  assessment_question assessment_question @relation(fields: [question_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_assessment_question_user_map_question")

  @@index([assessment_id], map: "fk_assessment_question_user_map_assessment")
  @@index([question_id], map: "fk_assessment_question_user_map_question_idx")
}

model assessment_responses {
  id                    Int       @id @default(autoincrement()) @db.UnsignedInt
  user_id               Int       @db.UnsignedInt
  question_id           Int       @db.UnsignedInt
  answer                String?   @db.Text
  require_note          String?   @db.Text
  notable_practice      Boolean?
  follow_up             Boolean?
  created_at            DateTime? @db.Timestamp(0)
  updated_at            DateTime? @db.Timestamp(0)
  uuid                  String?   @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  implementation_detail String?   @db.Text
  uuid_user_id          String?   @db.Char(36)
  uuid_question_id      String?   @db.Char(36)

  @@index([question_id], map: "idx_assessment_question")
}

model assessment_subcategory {
  id                           Int                            @id @default(autoincrement()) @db.UnsignedInt
  category_id                  Int                            @db.UnsignedInt
  subcategory                  String                         @db.VarChar(200)
  created_at                   DateTime?                      @db.Timestamp(0)
  updated_at                   DateTime?                      @db.Timestamp(0)
  uuid                         String?                        @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  uuid_category_id             String?                        @db.Char(36)
  assessment_category          assessment_category            @relation(fields: [category_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_assessment_subcategory_category")
  assessment_template_question assessment_template_question[]

  @@index([category_id], map: "fk_assessment_subcategory_category_idx")
}

model assessment_template {
  id                           Int                            @id @default(autoincrement()) @db.UnsignedInt
  uuid                         String                         @db.Char(36)
  organization_id              Int?                           @db.UnsignedInt
  partner_id                   Int?                           @db.UnsignedInt
  version                      String
  name                         String
  assign                       Boolean?
  request_evidence             Boolean?
  description                  String?                        @db.Text
  framework_id                 Int?                           @db.UnsignedInt
  level                        String?
  created_at                   DateTime?                      @db.Timestamp(0)
  updated_at                   DateTime?                      @db.Timestamp(0)
  deleted_at                   DateTime?                      @db.Timestamp(0)
  hidden                       Int                            @default(0)
  uuid_organization_id         String?                        @db.Char(36)
  uuid_partner_id              String?                        @db.Char(36)
  uuid_framework_id            String?                        @db.Char(36)
  is_standard                  Boolean                        @default(false)
  framework                    framework?                     @relation(fields: [framework_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_assessment_template_framework")
  organization                 organization?                  @relation(fields: [organization_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_assessment_template_organization")
  partner                      partner?                       @relation(fields: [partner_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_assessment_template_partner")
  assessment_template_question assessment_template_question[]

  @@index([framework_id], map: "fk_assessment_template_framework_idx")
  @@index([organization_id], map: "fk_assessment_template_organization")
  @@index([partner_id], map: "fk_assessment_template_partner")
}

model assessment_template_question {
  id                                      Int                                       @id @default(autoincrement()) @db.UnsignedInt
  assessment_template_id                  Int                                       @db.UnsignedInt
  question                                String?                                   @db.Text
  objective_criteria                      String?
  help_text                               String?                                   @db.Text
  category_id                             Int?                                      @db.UnsignedInt
  subcategory_id                          Int?                                      @db.UnsignedInt
  assign_user_id                          Int?                                      @db.UnsignedInt
  autoscore                               Boolean?
  autotask                                Boolean?
  require_note                            Boolean?
  require_notable_practice                Boolean?
  follow_up                               Boolean?
  request_evidence                        Boolean?
  response_type                           String?
  response_json                           String?                                   @db.Text
  ssp_report_field                        String?
  created_at                              DateTime?                                 @db.Timestamp(0)
  updated_at                              DateTime?                                 @db.Timestamp(0)
  scoring_weight                          Int?
  uuid                                    String?                                   @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  is_parent                               Boolean                                   @default(false)
  slug_identifier                         String?                                   @db.Text
  deleted_at                              DateTime?                                 @db.Timestamp(0)
  uuid_assessment_template_id             String?                                   @db.Char(36)
  uuid_category_id                        String?                                   @db.Char(36)
  uuid_subcategory_id                     String?                                   @db.Char(36)
  uuid_assign_user_id                     String?                                   @db.Char(36)
  assessment_template                     assessment_template                       @relation(fields: [assessment_template_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_assessment_template_question_assessment_template")
  assessment_category                     assessment_category?                      @relation(fields: [category_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_assessment_template_question_category")
  assessment_subcategory                  assessment_subcategory?                   @relation(fields: [subcategory_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_assessment_template_question_subcategory")
  assessment_template_question_subcontrol assessment_template_question_subcontrol[]

  @@index([assessment_template_id])
  @@index([category_id], map: "fk_assessment_template_question_category")
  @@index([subcategory_id], map: "fk_assessment_template_question_subcategory_idx")
}

model assessment_template_question_subcontrol {
  id                           Int                          @id @default(autoincrement()) @db.UnsignedInt
  question_id                  Int                          @db.UnsignedInt
  sub_control_template_id      Int                          @db.UnsignedInt
  created_at                   DateTime?                    @db.Timestamp(0)
  updated_at                   DateTime?                    @db.Timestamp(0)
  uuid                         String?                      @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  uuid_question_id             String?                      @db.Char(36)
  uuid_sub_control_template_id String?                      @db.Char(36)
  assessment_template_question assessment_template_question @relation(fields: [question_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_assessment_template_question_subcontrol_control")

  @@index([question_id], map: "fk_assessment_template_question_subcontrol_control")
}

model assessment_user_maps {
  id                 BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  assessment_id      Int
  user_id            Int
  created_at         DateTime? @db.Timestamp(0)
  updated_at         DateTime? @db.Timestamp(0)
  uuid_assessment_id String?   @db.Char(36)
  uuid_user_id       String?   @db.Char(36)

  @@index([assessment_id], map: "assessment_user_maps_assessment_id_index")
  @@index([user_id], map: "assessment_user_maps_user_id_index")
}

model audit {
  id                   Int           @id @unique(map: "id_UNIQUE") @default(autoincrement()) @db.UnsignedInt
  organization_id      Int?          @db.UnsignedInt
  title                String
  audit_firm           String?
  auditor_contact      String?
  framework_id         Int           @db.UnsignedInt
  level                String?
  auditor_id           Int           @db.UnsignedInt
  start_date           DateTime      @db.Date
  end_date             DateTime?     @db.Date
  filename             String?
  final_report         String?
  s3_bucket            String        @default("AWS_S3_BUCKET_STORAGE")
  s3_driver            String        @default("s3_storage")
  confirm_end_date     Int           @default(0) @db.TinyInt
  active               Boolean       @default(false)
  status               String?
  created_at           DateTime?     @db.Timestamp(0)
  updated_at           DateTime?     @db.Timestamp(0)
  deleted_at           DateTime?     @db.Timestamp(0)
  uuid                 String?       @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  uuid_organization_id String?       @db.Char(36)
  uuid_framework_id    String?       @db.Char(36)
  uuid_auditor_id      String?       @db.Char(36)
  framework            framework     @relation(fields: [framework_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_audit_framework")
  audit_users          audit_users[]
  task                 task[]

  @@index([framework_id], map: "fk_audit_framework_idx")
  @@index([auditor_id], map: "fk_audit_user_idx")
  @@index([organization_id], map: "idx_organization_id")
}

model audit_activities {
  id         Int       @id @unique(map: "id_UNIQUE") @default(autoincrement()) @db.UnsignedInt
  activity   String
  slug       String
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
  uuid       String?   @unique(map: "uuid_UNIQUE") @db.VarChar(45)
}

model audit_activity_user_map {
  id               Int        @id @default(autoincrement()) @db.UnsignedInt
  activity_id      Int        @db.UnsignedInt
  ticket_id        Int        @db.UnsignedInt
  user_id          Int        @db.UnsignedInt
  action           String?
  new_value        String?
  old_value        String?
  performed_at     DateTime   @db.DateTime(0)
  created_at       DateTime?  @db.Timestamp(0)
  updated_at       DateTime?  @db.Timestamp(0)
  uuid             String?    @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  uuid_activity_id String?    @db.Char(36)
  uuid_ticket_id   String?    @db.Char(36)
  uuid_user_id     String?    @db.Char(36)
  activities       activities @relation(fields: [activity_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_audit_activity_user_map_activity")
  user             user       @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_audit_activity_user_map_user")

  @@index([activity_id], map: "fk_audit_activity_user_map_activity_idx")
  @@index([user_id], map: "fk_audit_activity_user_map_user_idx")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model audit_control_activity_map {
  audit_request_id         Int?    @db.UnsignedInt
  control_activity_id      Int?    @db.UnsignedInt
  audit_id                 Int?    @db.UnsignedInt
  task_id                  Int?    @db.UnsignedInt
  uuid_audit_request_id    String? @db.Char(36)
  uuid_control_activity_id String? @db.Char(36)
  uuid_audit_id            String? @db.Char(36)
  uuid_task_id             String? @db.Char(36)

  @@index([audit_request_id], map: "idx_audit_request_id")
  @@index([control_activity_id], map: "idx_control_activity")
  @@index([task_id], map: "idx_task_id")
  @@ignore
}

model audit_request {
  id                     Int                    @id @unique(map: "id_UNIQUE") @default(autoincrement()) @db.UnsignedInt
  user_id                Int                    @db.UnsignedInt
  organization_id        Int?                   @db.UnsignedInt
  audit_id               Int?                   @db.UnsignedInt
  framework_id           Int                    @db.UnsignedInt
  control_id             Int                    @db.UnsignedInt
  unique_request_id      String?                @db.VarChar(51)
  status                 String
  subcontrol_id          Int                    @db.UnsignedInt
  control_description    String?                @db.Text
  evidence_request       String?                @db.Text
  due_date               DateTime?              @db.Date
  evidence_link          String?                @db.Text
  created_at             DateTime?              @db.Timestamp(0)
  updated_at             DateTime?              @db.Timestamp(0)
  deleted_at             DateTime?              @db.DateTime(0)
  criteria               String?
  tests_of_controls      String?
  control_in_place       String?
  notes                  String?                @db.Text
  recommendations        String?                @db.Text
  uuid                   String?                @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  hours                  Int                    @default(0)
  uuid_user_id           String?                @db.Char(36)
  uuid_organization_id   String?                @db.Char(36)
  uuid_audit_id          String?                @db.Char(36)
  uuid_framework_id      String?                @db.Char(36)
  uuid_control_id        String?                @db.Char(36)
  uuid_unique_request_id String?                @db.Char(36)
  uuid_subcontrol_id     String?                @db.Char(36)
  audit_request_assign   audit_request_assign[]

  @@index([audit_id], map: "audit_id")
  @@index([organization_id], map: "organization_id")
  @@index([subcontrol_id], map: "subcontrol_id")
  @@index([user_id], map: "user_id")
}

model audit_request_assign {
  id                    Int            @id @unique(map: "id_UNIQUE") @default(autoincrement()) @db.UnsignedInt
  audit_request_id      Int?           @db.UnsignedInt
  assign_to_id          Int?           @db.UnsignedInt
  created_at            DateTime?      @db.Timestamp(0)
  updated_at            DateTime?      @db.Timestamp(0)
  uuid                  String?        @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  uuid_audit_request_id String?        @db.Char(36)
  uuid_assign_to_id     String?        @db.Char(36)
  audit_request         audit_request? @relation(fields: [audit_request_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_audit_request_assign_audit_request")

  @@index([audit_request_id], map: "fk_audit_request_assign_audit_request_idx")
  @@index([audit_request_id], map: "idx_audit_request")
}

model audit_request_risks {
  id                    BigInt  @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                  String? @unique(map: "audit_request_risks_uuid_unique") @db.Char(36)
  audit_id              Int
  audit_request_id      Int
  risk_id               Int
  uuid_audit_id         String? @db.Char(36)
  uuid_audit_request_id String? @db.Char(36)
  uuid_risk_id          String? @db.Char(36)

  @@index([audit_id], map: "idx_audit_id")
  @@index([audit_request_id], map: "idx_audit_request")
}

model audit_task_comment_users {
  id                   Int       @id @unique(map: "id_UNIQUE") @default(autoincrement()) @db.UnsignedInt
  task_id              Int       @db.UnsignedInt
  task_comment_id      Int       @db.UnsignedInt
  user_id              Int       @db.UnsignedInt
  receiver_id          Int       @db.UnsignedInt
  created_at           DateTime? @db.Timestamp(0)
  updated_at           DateTime? @db.Timestamp(0)
  uuid                 String?   @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  uuid_task_id         String?   @db.Char(36)
  uuid_task_comment_id String?   @db.Char(36)
  uuid_user_id         String?   @db.Char(36)
  uuid_receiver_id     String?   @db.Char(36)
  task                 task      @relation(fields: [task_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_audit_task_comment_users_task")
  user                 user      @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_audit_task_comment_users_user")

  @@index([task_id], map: "fk_audit_task_comment_users_task_idx")
  @@index([user_id], map: "fk_audit_task_comment_users_user_idx")
}

model audit_users {
  id            Int       @id @unique(map: "id_UNIQUE") @default(autoincrement()) @db.UnsignedInt
  audit_id      Int       @db.UnsignedInt
  user_id       Int       @db.UnsignedInt
  created_at    DateTime? @db.Timestamp(0)
  updated_at    DateTime? @db.Timestamp(0)
  uuid          String?   @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  uuid_audit_id String?   @db.Char(36)
  uuid_user_id  String?   @db.Char(36)
  audit         audit     @relation(fields: [audit_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_audit_users_audit")
  user          user      @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_audit_users_user")

  @@index([audit_id], map: "fk_audit_users_audit_idx")
  @@index([user_id], map: "fk_audit_users_user_idx")
}

model aws_customer_product {
  id                           Int       @id @default(autoincrement())
  uuid                         String?   @unique(map: "aws_customer_product_uuid_unique") @db.Char(36)
  customer_identifier          String    @db.VarChar(255)
  customer_aws_account_id      String    @db.VarChar(255)
  product_code                 String    @db.VarChar(255)
  hubspot_contact_id           String?
  created_at                   DateTime  @default(now()) @db.DateTime(6)
  updated_at                   DateTime  @default(now()) @db.DateTime(6)
  deleted_at                   DateTime? @db.DateTime(6)
  organization_id              Int?
  entitlements                 Json?
  uuid_customer_aws_account_id String?   @db.Char(36)
  uuid_hubspot_contact_id      String?   @db.Char(36)
  uuid_organization_id         String?   @db.Char(36)
}

model blank_task_packs {
  id                Int      @id @default(autoincrement())
  uuid              String?  @unique(map: "blank_task_packs_uuid_unique") @db.Char(36)
  framework_id      Int?
  app               String   @db.VarChar(255)
  subcontrol        String   @db.VarChar(255)
  task_subject      String   @db.VarChar(500)
  task_actions      String?  @db.VarChar(1500)
  created_at        DateTime @default(now()) @db.DateTime(0)
  updated_at        DateTime @default(now()) @db.DateTime(0)
  framework         String?  @db.VarChar(255)
  uuid_framework_id String?  @db.Char(36)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model bundle_bundles_map {
  parent_id                                     String           @db.Char(36)
  child_id                                      String           @db.Char(36)
  bundle_setting_id                             String?          @db.Char(36)
  created_at                                    DateTime?        @db.Timestamp(0)
  updated_at                                    DateTime?        @db.Timestamp(0)
  uuid_parent_id                                String?          @db.Char(36)
  uuid_child_id                                 String?          @db.Char(36)
  uuid_bundle_setting_id                        String?          @db.Char(36)
  bundle_settings                               bundle_settings? @relation(fields: [bundle_setting_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "bundle_bundles_map_bundle_setting_id_foreign")
  bundles_bundle_bundles_map_child_idTobundles  bundles          @relation("bundle_bundles_map_child_idTobundles", fields: [child_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "bundle_bundles_map_child_id_foreign")
  bundles_bundle_bundles_map_parent_idTobundles bundles          @relation("bundle_bundles_map_parent_idTobundles", fields: [parent_id], references: [id], onUpdate: Restrict, map: "bundle_bundles_map_parent_id_foreign")

  @@index([bundle_setting_id], map: "bundle_bundles_map_bundle_setting_id_foreign")
  @@index([child_id], map: "bundle_bundles_map_child_id_foreign")
  @@index([parent_id], map: "bundle_bundles_map_parent_id_foreign")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model bundle_rule_map {
  bundle_id      String  @db.Char(36)
  rule_id        String  @db.Char(36)
  uuid_bundle_id String? @db.Char(36)
  uuid_rule_id   String? @db.Char(36)
  bundles        bundles @relation(fields: [bundle_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "bundle_rule_map_bundle_id_foreign")
  rule           rule    @relation(fields: [rule_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "bundle_rule_map_rule_id_foreign")

  @@index([bundle_id], map: "bundle_rule_map_bundle_id_foreign")
  @@index([rule_id], map: "bundle_rule_map_rule_id_foreign")
  @@ignore
}

model bundle_settings {
  id                 String               @id @db.Char(36)
  uuid               String?              @unique(map: "bundle_settings_uuid_unique") @db.Char(36)
  bundle_id          String?              @db.Char(36)
  parameters         String?
  created_at         DateTime?            @db.Timestamp(0)
  updated_at         DateTime?            @db.Timestamp(0)
  deleted_at         DateTime?            @db.Timestamp(0)
  uuid_bundle_id     String?              @db.Char(36)
  bundle_bundles_map bundle_bundles_map[] @ignore
  bundles            bundles?             @relation(fields: [bundle_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "bundle_settings_bundle_id_foreign")

  @@index([bundle_id], map: "bundle_settings_bundle_id_foreign")
}

model bundles {
  id                                                       String                         @id @db.Char(36)
  uuid                                                     String?                        @unique(map: "bundles_uuid_unique") @db.Char(36)
  name                                                     String
  type                                                     String
  sequence                                                 Int                            @default(0)
  slug                                                     String
  created_at                                               DateTime?                      @db.Timestamp(0)
  updated_at                                               DateTime?                      @db.Timestamp(0)
  deleted_at                                               DateTime?                      @db.Timestamp(0)
  parent_id                                                String?                        @db.Char(36)
  help_text                                                String?                        @db.Text
  uuid_parent_id                                           String?                        @db.Char(36)
  frontend_config                                          Json?
  related_resources                                        String?
  resource_config                                          Json?
  bundle_bundles_map_bundle_bundles_map_child_idTobundles  bundle_bundles_map[]           @relation("bundle_bundles_map_child_idTobundles") @ignore
  bundle_bundles_map_bundle_bundles_map_parent_idTobundles bundle_bundles_map[]           @relation("bundle_bundles_map_parent_idTobundles") @ignore
  bundle_rule_map                                          bundle_rule_map[]              @ignore
  bundle_settings                                          bundle_settings[]
  bundles                                                  bundles?                       @relation("bundlesTobundles", fields: [parent_id], references: [id], onUpdate: Restrict, map: "product_parent_id_foreign")
  other_bundles                                            bundles[]                      @relation("bundlesTobundles")
  organization_products                                    organization_products[]        @ignore
  partner                                                  partner[]
  partner_bundle_map                                       partner_bundle_map[]           @ignore
  subscription_type_bundle_map                             subscription_type_bundle_map[] @ignore

  @@index([parent_id], map: "product_parent_id_foreign")
}

model bundles_entity_log {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid                String?   @unique(map: "bundles_entity_log_uuid_unique") @db.Char(36)
  type                String    @db.VarChar(10)
  object_id           String    @db.VarChar(255)
  discriminator       String?   @db.VarChar(255)
  transaction_hash    String?   @db.VarChar(40)
  diffs               String?   @db.Text
  blame_id            String?   @db.VarChar(255)
  blame_user          String?   @db.VarChar(255)
  blame_user_fqdn     String?   @db.VarChar(255)
  blame_user_firewall String?   @db.VarChar(100)
  ip                  String?   @db.VarChar(45)
  created_at          DateTime? @db.Timestamp(0)
  updated_at          DateTime? @db.Timestamp(0)
  uuid_object_id      String?   @db.Char(36)
  uuid_blame_id       String?   @db.Char(36)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model cmmc_level_3_mappings {
  apptega_control String? @db.Text
  cmmc            String? @db.Text
  parent_id       Int?
  sub_id          Int?
  uuid_parent_id  String? @db.Char(36)
  uuid_sub_id     String? @db.Char(36)

  @@ignore
}

model connections_fields {
  id                Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid              String?   @unique(map: "connections_fields_uuid_unique") @db.Char(36)
  connector_id      Int
  field_name        String
  help_text         String?   @db.Text
  type              String
  order             Int
  created_at        DateTime? @db.Timestamp(0)
  updated_at        DateTime? @db.Timestamp(0)
  uuid_connector_id String?   @db.Char(36)
}

model connector_subcontrols {
  id                     Int             @id @default(autoincrement()) @db.UnsignedInt
  uuid                   String?         @unique(map: "connector_subcontrols_uuid_unique") @db.Char(36)
  connector_id           Int             @db.UnsignedInt
  subcontrol_id          Int             @db.UnsignedInt
  user_connector_id      Int             @db.UnsignedInt
  created_at             DateTime?       @db.Timestamp(0)
  updated_at             DateTime?       @db.Timestamp(0)
  deleted_at             DateTime?       @db.Timestamp(0)
  uuid_connector_id      String?         @db.Char(36)
  uuid_subcontrol_id     String?         @db.Char(36)
  uuid_user_connector_id String?         @db.Char(36)
  connectors             connectors      @relation(fields: [connector_id], references: [id], onUpdate: Restrict, map: "connector_subcontrols_connector_id_foreign")
  sub_control            sub_control     @relation(fields: [subcontrol_id], references: [id], onUpdate: Restrict, map: "connector_subcontrols_subcontrol_id_foreign")
  user_connectors        user_connectors @relation(fields: [user_connector_id], references: [id], onUpdate: Restrict, map: "connector_subcontrols_user_connector_id_foreign")

  @@index([connector_id], map: "connector_subcontrols_connector_id_foreign")
  @@index([subcontrol_id], map: "connector_subcontrols_subcontrol_id_foreign")
  @@index([user_connector_id], map: "connector_subcontrols_user_connector_id_foreign")
}

model connectors {
  id                              Int                               @id @default(autoincrement()) @db.UnsignedInt
  uuid                            String?                           @unique(map: "connectors_uuid_unique") @db.Char(36)
  name                            String
  slug                            String
  category                        String
  image                           String?
  s3_bucket                       String                            @default("AWS_S3_BUCKET_STORAGE")
  s3_driver                       String                            @default("s3_storage")
  description                     String?                           @db.Text
  documentation_link              Json?
  is_active                       Boolean                           @default(false)
  order                           Int?
  type                            String?                           @default("native")
  api_url                         String?
  status                          String?                           @default("active")
  auth0_authentication            Boolean                           @default(true)
  created_at                      DateTime?                         @db.Timestamp(0)
  updated_at                      DateTime?                         @db.Timestamp(0)
  deleted_at                      DateTime?                         @db.Timestamp(0)
  required_bundle                 String?
  required_feature_flag           String?
  connector_subcontrols           connector_subcontrols[]
  organization_connector          organization_connector[]
  task_comments_connector_mapping task_comments_connector_mapping[]
}

model connectors_entity_log {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid                String?   @unique(map: "connectors_entity_log_uuid_unique") @db.Char(36)
  type                String    @db.VarChar(10)
  object_id           String    @db.VarChar(255)
  discriminator       String?   @db.VarChar(255)
  transaction_hash    String?   @db.VarChar(40)
  diffs               String?   @db.Text
  blame_id            String?   @db.VarChar(255)
  blame_user          String?   @db.VarChar(255)
  blame_user_fqdn     String?   @db.VarChar(255)
  blame_user_firewall String?   @db.VarChar(100)
  ip                  String?   @db.VarChar(45)
  created_at          DateTime? @db.Timestamp(0)
  updated_at          DateTime? @db.Timestamp(0)
  uuid_object_id      String?   @db.Char(36)
  uuid_blame_id       String?   @db.Char(36)
}

model contact_request {
  id                Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid              String?   @unique(map: "contact_request_uuid_unique") @db.Char(36)
  from_user_id      Int       @db.UnsignedInt
  title             String    @db.Text
  message           String    @db.Text
  type              Int       @db.TinyInt
  created_at        DateTime? @db.Timestamp(0)
  updated_at        DateTime? @db.Timestamp(0)
  uuid_from_user_id String?   @db.Char(36)
  user              user      @relation(fields: [from_user_id], references: [id], onUpdate: Restrict, map: "contact_request_from_user_id_foreign")

  @@index([from_user_id], map: "contact_request_from_user_id_foreign")
}

model control_activity {
  id                   Int                         @id @default(autoincrement()) @db.UnsignedInt
  uuid                 String?                     @unique(map: "control_activity_uuid_unique") @db.Char(36)
  name                 String
  description          String                      @db.Text
  organization_id      Int                         @db.UnsignedInt
  created_at           DateTime?                   @db.Timestamp(0)
  updated_at           DateTime?                   @db.Timestamp(0)
  deleted_at           DateTime?                   @db.Timestamp(0)
  is_display           Boolean                     @default(true)
  frequency            control_activity_frequency?
  uuid_organization_id String?                     @db.Char(36)

  @@index([organization_id], map: "idx_org_id")
}

model crud_logs {
  id                BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid              String?   @unique(map: "crud_logs_uuid_unique") @db.Char(36)
  user_type         String?
  user_id           BigInt?   @db.UnsignedBigInt
  event             String
  auditable_type    String
  auditable_id      BigInt    @db.UnsignedBigInt
  old_values        String?   @db.Text
  new_values        String?   @db.Text
  url               String?   @db.Text
  ip_address        String?   @db.VarChar(45)
  user_agent        String?   @db.VarChar(1023)
  tags              String?
  created_at        DateTime? @db.Timestamp(0)
  updated_at        DateTime? @db.Timestamp(0)
  uuid_user_id      String?   @db.Char(36)
  uuid_auditable_id String?   @db.Char(36)

  @@index([auditable_type, auditable_id], map: "crud_logs_auditable_type_auditable_id_index")
  @@index([user_id, user_type], map: "crud_logs_user_id_user_type_index")
}

model db_logs {
  id            Int       @id @default(autoincrement())
  uuid          String?   @unique(map: "db_logs_uuid_unique") @db.Char(36)
  type          String?   @db.VarChar(10)
  user_name     String?   @db.VarChar(50)
  table         String?
  table_id      Int?
  field         String?
  old_value     String?
  new_value     String?
  timestamp     DateTime? @db.DateTime(0)
  uuid_table_id String?   @db.Char(36)
}

model default_scoring_keys {
  id          Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid        String?   @unique(map: "default_scoring_keys_uuid_unique") @db.Char(36)
  name        String
  description String    @db.Text
  order       Int
  created_at  DateTime? @db.Timestamp(0)
  updated_at  DateTime? @db.Timestamp(0)
}

model document {
  id                             Int                            @id @default(autoincrement()) @db.UnsignedInt
  uuid                           String?                        @unique(map: "document_uuid_unique") @db.Char(36)
  sub_control_id                 Int?                           @db.UnsignedInt
  organization_id                Int                            @db.UnsignedInt
  title                          String
  file_path                      String?
  link                           String?                        @db.Text
  s3_bucket                      String                         @default("AWS_S3_BUCKET_STORAGE")
  s3_driver                      String                         @default("s3_storage")
  type                           String?
  evidence                       Boolean?
  mime_type                      String?
  hash                           String?
  is_display                     Boolean                        @default(true)
  filesize                       Decimal?                       @db.Decimal(14, 2)
  created_at                     DateTime?                      @db.Timestamp(0)
  updated_at                     DateTime?                      @db.Timestamp(0)
  deleted_at                     DateTime?                      @db.DateTime(0)
  dedupe                         String?
  created_by                     Int?                           @db.UnsignedInt
  updated_by                     Int?                           @db.UnsignedInt
  uuid_sub_control_id            String?                        @db.Char(36)
  uuid_organization_id           String?                        @db.Char(36)
  uuid_created_by                String?                        @db.Char(36)
  uuid_updated_by                String?                        @db.Char(36)
  user_document_created_byTouser user?                          @relation("document_created_byTouser", fields: [created_by], references: [id], onUpdate: NoAction, map: "document_created_by_foreign")
  user_document_updated_byTouser user?                          @relation("document_updated_byTouser", fields: [updated_by], references: [id], onUpdate: NoAction, map: "document_updated_by_foreign")
  document_document_categories   document_document_categories[] @ignore

  @@index([created_by], map: "document_created_by_foreign")
  @@index([hash, link(length: 191)], map: "document_hash_idx")
  @@index([organization_id])
  @@index([id, title, sub_control_id], map: "document_search")
  @@index([sub_control_id], map: "document_sub_control_id_foreign")
  @@index([updated_by], map: "document_updated_by_foreign")
}

model document_categories {
  id                               String                             @id @db.Char(36)
  uuid                             String?                            @unique(map: "document_categories_uuid_unique") @db.Char(36)
  name                             String                             @unique(map: "document_categories_name_unique")
  created_at                       DateTime?                          @db.Timestamp(0)
  updated_at                       DateTime?                          @db.Timestamp(0)
  is_system                        Boolean                            @default(false)
  document_document_categories     document_document_categories[]     @ignore
  organization_document_categories organization_document_categories[] @ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model document_document_categories {
  document_id               Int                 @db.UnsignedInt
  document_category_id      String              @db.Char(36)
  created_at                DateTime?           @db.Timestamp(0)
  updated_at                DateTime?           @db.Timestamp(0)
  uuid_document_id          String?             @db.Char(36)
  uuid_document_category_id String?             @db.Char(36)
  document_categories       document_categories @relation(fields: [document_category_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "document_document_categories_document_category_id_foreign")
  document                  document            @relation(fields: [document_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "document_document_categories_document_id_foreign")

  @@index([document_category_id], map: "document_document_categories_document_category_id_foreign")
  @@index([document_id], map: "document_document_categories_document_id_foreign")
  @@ignore
}

model document_questions {
  id               BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid             String?   @unique(map: "document_questions_uuid_unique") @db.Char(36)
  document_id      Int
  question_id      Int
  user_id          Int
  created_at       DateTime? @db.Timestamp(0)
  updated_at       DateTime? @db.Timestamp(0)
  uuid_document_id String?   @db.Char(36)
  uuid_question_id String?   @db.Char(36)
  uuid_user_id     String?   @db.Char(36)
}

model document_sub_controls {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid                String?   @unique(map: "document_sub_controls_uuid_unique") @db.Char(36)
  document_id         Int       @db.UnsignedInt
  sub_control_id      Int       @db.UnsignedInt
  user_id             Int?      @db.UnsignedInt
  level               String?
  created_at          DateTime? @db.Timestamp(0)
  updated_at          DateTime? @db.Timestamp(0)
  key                 String?   @db.VarChar(1000)
  uuid_document_id    String?   @db.Char(36)
  uuid_sub_control_id String?   @db.Char(36)
  uuid_user_id        String?   @db.Char(36)

  @@index([document_id], map: "document_sub_controls_document_id_foreign")
  @@index([sub_control_id], map: "sub_control_id")
}

model document_tasks {
  id                           Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid                         String?   @unique(map: "document_tasks_uuid_unique") @db.Char(36)
  document_id                  Int       @db.UnsignedInt
  task_id                      Int       @db.UnsignedInt
  user_id                      Int       @db.UnsignedInt
  connector_attachment_id      String?
  created_at                   DateTime? @db.Timestamp(0)
  updated_at                   DateTime? @db.Timestamp(0)
  uuid_document_id             String?   @db.Char(36)
  uuid_task_id                 String?   @db.Char(36)
  uuid_user_id                 String?   @db.Char(36)
  uuid_connector_attachment_id String?   @db.Char(36)
  task                         task      @relation(fields: [task_id], references: [id], onUpdate: Restrict, map: "document_tasks_task_id_foreign")

  @@index([document_id], map: "document_tasks_document_id_foreign")
  @@index([task_id], map: "document_tasks_task_id_foreign")
}

model entity_looker_dashboard {
  id                String    @id @default(dbgenerated("(uuid())")) @db.Char(36)
  uuid              String?   @unique(map: "entity_looker_dashboard_uuid_unique") @db.Char(36)
  dashboard_id      String    @db.VarChar(255)
  entity_type       String    @db.VarChar(255)
  entity_id         String?   @db.VarChar(255)
  created_at        DateTime? @db.Timestamp(0)
  updated_at        DateTime? @db.Timestamp(0)
  uuid_dashboard_id String?   @db.Char(36)
  uuid_entity_id    String?   @db.Char(36)
}

model failed_job {
  id         BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  uuid       String?  @unique(map: "failed_job_uuid_unique") @db.Char(36)
  connection String   @db.Text
  queue      String   @db.Text
  payload    String   @db.LongText
  exception  String   @db.LongText
  failed_at  DateTime @default(now()) @db.Timestamp(0)
}

model field_permission {
  id                        Int                         @id @default(autoincrement()) @db.UnsignedInt
  uuid                      String?                     @unique(map: "field_permission_uuid_unique") @db.Char(36)
  name                      String
  code                      String
  created_at                DateTime?                   @db.Timestamp(0)
  updated_at                DateTime?                   @db.Timestamp(0)
  role_field_permission_map role_field_permission_map[] @ignore
}

model form {
  id                        Int         @id @default(autoincrement()) @db.UnsignedInt
  organization_id           Int?
  partner_id                Int?
  template                  Boolean     @default(false)
  status                    form_status @default(active)
  type                      String?     @default("vendor")
  title                     String?     @db.Text
  description               String?     @db.Text
  author_id                 Int?        @db.UnsignedInt
  available_to_organization Boolean     @default(false)
  created_at                DateTime?   @db.Timestamp(0)
  updated_at                DateTime?   @db.Timestamp(0)
  deleted_at                DateTime?   @db.Timestamp(0)
  form_id                   Int?        @db.UnsignedInt
  version                   Int?        @db.UnsignedInt
  uuid                      String      @db.Char(36)
  hidden                    Int         @default(0)
  uuid_organization_id      String?     @db.Char(36)
  uuid_partner_id           String?     @db.Char(36)
  uuid_author_id            String?     @db.Char(36)
  uuid_form_id              String?     @db.Char(36)
  questionnaire_score       Float?
  has_scoring               Boolean     @default(false)

  @@index([author_id], map: "idx_author")
  @@index([organization_id], map: "idx_org")
  @@index([partner_id], map: "idx_partner")
}

model form_field {
  id                   Int              @id @default(autoincrement()) @db.UnsignedInt
  uuid                 String?          @unique(map: "form_field_uuid_unique") @db.Char(36)
  organization_id      Int?
  partner_id           Int?
  form_id              Int
  form_group_id        Int
  form_field_id        Int?             @db.UnsignedInt
  title                String?          @db.Text
  identifier           String?
  type                 form_field_type?
  options              String?          @db.Text
  config               String?          @db.Text
  help_text            String?          @db.VarChar(500)
  default_response     Int?
  request_evidence     Int              @default(0) @db.TinyInt
  sort_order           Int?
  created_at           DateTime?        @db.Timestamp(0)
  updated_at           DateTime?        @db.Timestamp(0)
  deleted_at           DateTime?        @db.Timestamp(0)
  is_required          Int              @default(0) @db.TinyInt
  uuid_organization_id String?          @db.Char(36)
  uuid_partner_id      String?          @db.Char(36)
  uuid_form_id         String?          @db.Char(36)
  uuid_form_group_id   String?          @db.Char(36)
  uuid_form_field_id   String?          @db.Char(36)
}

model form_field_response {
  id                    Int                       @id @default(autoincrement()) @db.UnsignedInt
  uuid                  String?                   @unique(map: "form_field_response_uuid_unique") @db.Char(36)
  organization_id       Int?
  partner_id            Int?
  form_instance_id      Int
  form_group_id         Int
  form_field_id         Int
  title                 String?                   @db.Text
  identifier            String?
  type                  form_field_response_type?
  config                String?                   @db.Text
  options               String                    @db.Text
  response              String?                   @db.Text
  request_evidence      Int                       @default(0) @db.TinyInt
  has_response          Int                       @default(0)
  sort_order            Int?
  created_at            DateTime?                 @db.Timestamp(0)
  updated_at            DateTime?                 @db.Timestamp(0)
  note                  String?                   @db.Text
  is_required           Int                       @default(0) @db.TinyInt
  uuid_organization_id  String?                   @db.Char(36)
  uuid_partner_id       String?                   @db.Char(36)
  uuid_form_instance_id String?                   @db.Char(36)
  uuid_form_group_id    String?                   @db.Char(36)
  uuid_form_field_id    String?                   @db.Char(36)

  @@index([organization_id], map: "organization_id")
  @@index([partner_id], map: "partner_id")
  @@index([type], map: "type")
}

model form_group {
  id                   BigInt                 @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                 String?                @unique(map: "form_group_uuid_unique") @db.Char(36)
  organization_id      Int?
  partner_id           Int?
  parent_id            Int
  parent_type          form_group_parent_type
  title                String
  sort_order           Int?
  created_at           DateTime?              @db.Timestamp(0)
  updated_at           DateTime?              @db.Timestamp(0)
  deleted_at           DateTime?              @db.Timestamp(0)
  form_id              BigInt                 @db.UnsignedBigInt
  uuid_organization_id String?                @db.Char(36)
  uuid_partner_id      String?                @db.Char(36)
  uuid_parent_id       String?                @db.Char(36)
  uuid_form_id         String?                @db.Char(36)
  group_score          Float?
}

model form_instance {
  id                   BigInt               @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                 String               @default("01f5477d-7723-4388-8ad6-f4b4597f0147") @db.Char(36)
  organization_id      Int?
  partner_id           Int?
  form_id              Int
  assignee_id          Int?                 @db.UnsignedInt
  status               form_instance_status @default(draft)
  title                String
  description          String?              @db.Text
  created_at           DateTime?            @db.Timestamp(0)
  updated_at           DateTime?            @db.Timestamp(0)
  hidden               Int                  @default(0)
  uuid_organization_id String?              @db.Char(36)
  uuid_partner_id      String?              @db.Char(36)
  uuid_form_id         String?              @db.Char(36)
  uuid_assignee_id     String?              @db.Char(36)
}

model framework {
  id                                Int                                 @id @default(autoincrement()) @db.UnsignedInt
  uuid                              String?                             @unique(map: "framework_uuid_unique") @db.Char(36)
  name                              String
  slug                              String?                             @db.VarChar(255)
  organization_id                   Int?                                @db.UnsignedInt
  has_mappings                      String                              @default("1")
  deprecated                        Boolean?                            @default(false)
  gov                               Boolean?                            @default(false)
  created_at                        DateTime?                           @db.Timestamp(0)
  updated_at                        DateTime?                           @db.Timestamp(0)
  deleted_at                        DateTime?                           @db.Timestamp(0)
  partner_id                        Int?                                @db.UnsignedInt
  is_harmony                        Boolean                             @default(false)
  description                       String?                             @db.Text
  has_objective_criteria            Boolean                             @default(false)
  has_multiple_levels               Boolean?
  default_level                     String?
  is_visible                        Boolean                             @default(true)
  uuid_organization_id              String?                             @db.Char(36)
  uuid_partner_id                   String?                             @db.Char(36)
  assessment_template               assessment_template[]
  audit                             audit[]
  organization                      organization?                       @relation(fields: [organization_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "framework_organization_id_foreign")
  framework_app_template_map        framework_app_template_map[]        @ignore
  framework_product_map             framework_product_map[]             @ignore
  insurance_products                insurance_products[]
  organization_predesign_frameworks organization_predesign_frameworks[]
  program                           program[]
  program_framework_map             program_framework_map[]             @ignore
  role_framework_map                role_framework_map[]                @ignore

  @@index([organization_id], map: "framework_organization_id_foreign")
  @@index([partner_id], map: "framework_partner_id_index")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model framework_app_template_map {
  framework_id         Int          @db.UnsignedInt
  app_template_id      Int          @db.UnsignedInt
  uuid_framework_id    String?      @db.Char(36)
  uuid_app_template_id String?      @db.Char(36)
  app_template         app_template @relation(fields: [app_template_id], references: [id], onUpdate: Restrict, map: "framework_app_template_map_app_template_id_foreign")
  framework            framework    @relation(fields: [framework_id], references: [id], onUpdate: Restrict, map: "framework_app_template_map_framework_id_foreign")

  @@index([app_template_id], map: "framework_app_template_map_app_template_id_foreign")
  @@index([framework_id], map: "framework_app_template_map_framework_id_foreign")
  @@ignore
}

model framework_documents {
  id                Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid              String?   @unique(map: "framework_documents_uuid_unique") @db.Char(36)
  framework_id      Int?      @db.UnsignedInt
  document_title    String
  document_url      String?
  created_at        DateTime? @db.Timestamp(0)
  updated_at        DateTime? @db.Timestamp(0)
  uuid_framework_id String?   @db.Char(36)
}

model framework_migration {
  id                    Int     @id @unique(map: "id_UNIQUE") @default(autoincrement()) @db.UnsignedInt
  uuid                  String? @unique(map: "framework_migration_uuid_unique") @db.Char(36)
  source_framework      String? @db.VarChar(255)
  destination_framework String? @db.VarChar(255)
  isactive              Boolean @default(dbgenerated("b'1'")) @db.Bit(1)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model framework_product_map {
  framework_id      Int       @db.UnsignedInt
  product_id        Int       @db.UnsignedInt
  uuid_framework_id String?   @db.Char(36)
  uuid_product_id   String?   @db.Char(36)
  framework         framework @relation(fields: [framework_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "framework_product_map_framework_id_foreign")
  product           product   @relation(fields: [product_id], references: [id], onUpdate: Restrict, map: "framework_product_map_product_id_foreign")

  @@index([framework_id], map: "framework_product_map_framework_id_foreign")
  @@index([product_id], map: "framework_product_map_product_id_foreign")
  @@ignore
}

model import_framework_staging {
  id            BigInt                                 @id @default(autoincrement()) @db.UnsignedBigInt
  uuid          String?                                @unique(map: "import_framework_staging_uuid_unique") @db.Char(36)
  content       String                                 @db.LongText
  type          import_framework_staging_type
  import_status import_framework_staging_import_status
  created_at    DateTime?                              @db.Timestamp(0)
  updated_at    DateTime?                              @db.Timestamp(0)
}

model insurance_applications {
  id                         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                       String    @db.Char(36)
  status                     String
  assessment_id              Int?      @db.UnsignedInt
  form_instance_id           Int?      @db.UnsignedInt
  user_id                    Int?      @db.UnsignedInt
  organization_id            Int       @db.UnsignedInt
  insurance_provider_id      Int       @db.UnsignedInt
  insurance_product_id       Int       @db.UnsignedInt
  created_at                 DateTime? @db.Timestamp(0)
  updated_at                 DateTime? @db.Timestamp(0)
  deleted_at                 DateTime? @db.Timestamp(0)
  form_uuid                  String?   @unique(map: "insurance_applications_form_uuid_unique") @db.Char(36)
  uuid_assessment_id         String?   @db.Char(36)
  uuid_form_instance_id      String?   @db.Char(36)
  uuid_user_id               String?   @db.Char(36)
  uuid_organization_id       String?   @db.Char(36)
  uuid_insurance_provider_id String?   @db.Char(36)
  uuid_insurance_product_id  String?   @db.Char(36)
}

model insurance_product_subcontrol_map {
  id                           BigInt               @id @default(autoincrement()) @db.UnsignedBigInt
  insurance_product_id         BigInt               @db.UnsignedBigInt
  sub_control_template_id      Int                  @db.UnsignedInt
  created_at                   DateTime?            @db.Timestamp(0)
  updated_at                   DateTime?            @db.Timestamp(0)
  uuid_insurance_product_id    String?              @db.Char(36)
  uuid_sub_control_template_id String?              @db.Char(36)
  insurance_products           insurance_products   @relation(fields: [insurance_product_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "insurance_product_subcontrol_map_insurance_product_id_foreign")
  sub_control_template         sub_control_template @relation(fields: [sub_control_template_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "insurance_product_subcontrol_map_sub_control_template_id_foreign")

  @@index([insurance_product_id], map: "insurance_product_subcontrol_map_insurance_product_id_foreign")
  @@index([sub_control_template_id], map: "insurance_product_subcontrol_map_sub_control_template_id_foreign")
}

model insurance_products {
  id                               BigInt                             @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                             String                             @db.Char(36)
  insurance_provider_id            Int                                @db.UnsignedInt
  framework_id                     Int?                               @db.UnsignedInt
  threshold                        Int                                @db.UnsignedInt
  name                             String
  description                      String                             @db.Text
  status                           String
  image                            String
  form_id                          Int?                               @db.UnsignedInt
  assessment_template_id           Int?                               @db.UnsignedInt
  created_at                       DateTime?                          @db.Timestamp(0)
  updated_at                       DateTime?                          @db.Timestamp(0)
  deleted_at                       DateTime?                          @db.Timestamp(0)
  form_uuid                        String?                            @unique(map: "insurance_products_form_uuid_unique") @db.Char(36)
  uuid_insurance_provider_id       String?                            @db.Char(36)
  uuid_framework_id                String?                            @db.Char(36)
  uuid_form_id                     String?                            @db.Char(36)
  uuid_assessment_template_id      String?                            @db.Char(36)
  insurance_product_subcontrol_map insurance_product_subcontrol_map[]
  framework                        framework?                         @relation(fields: [framework_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "insurance_products_framework_id_foreign")

  @@index([framework_id], map: "insurance_products_framework_id_index")
}

model insurance_providers {
  id                   BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                 String    @db.Char(36)
  name                 String
  slug                 String?
  description          String    @db.Text
  status               String
  image                String
  icon                 String?
  created_at           DateTime? @db.Timestamp(0)
  updated_at           DateTime? @db.Timestamp(0)
  deleted_at           DateTime? @db.Timestamp(0)
  delivery_format      String?
  delivery_action      String?
  delivery_destination String?
  delivery_config      Json?
}

model job {
  id           BigInt  @id @default(autoincrement()) @db.UnsignedBigInt
  uuid         String? @unique(map: "job_uuid_unique") @db.Char(36)
  queue        String
  payload      String  @db.LongText
  attempts     Int     @db.UnsignedTinyInt
  reserved_at  Int?    @db.UnsignedInt
  available_at Int     @db.UnsignedInt
  created_at   Int     @db.UnsignedInt

  @@index([queue, reserved_at], map: "job_queue_reserved_at_index")
}

model jobs {
  id           BigInt  @id @default(autoincrement()) @db.UnsignedBigInt
  uuid         String? @unique(map: "jobs_uuid_unique") @db.Char(36)
  queue        String
  payload      String  @db.LongText
  attempts     Int     @db.UnsignedTinyInt
  reserved_at  Int?    @db.UnsignedInt
  available_at Int     @db.UnsignedInt
  created_at   Int     @db.UnsignedInt

  @@index([queue, reserved_at], map: "jobs_queue_reserved_at_index")
}

model login_log {
  id                   Int           @id @default(autoincrement()) @db.UnsignedInt
  uuid                 String?       @unique(map: "login_log_uuid_unique") @db.Char(36)
  user_id              Int           @db.UnsignedInt
  login_time           DateTime      @db.DateTime(0)
  is_auto_login        Boolean?
  organization_id      Int?          @db.UnsignedInt
  uuid_user_id         String?       @db.Char(36)
  uuid_organization_id String?       @db.Char(36)
  organization         organization? @relation(fields: [organization_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "login_log_organization_id_foreign")
  user                 user          @relation(fields: [user_id], references: [id], onUpdate: Restrict, map: "login_log_user_id_foreign")

  @@index([organization_id], map: "login_log_organization_id_foreign")
  @@index([user_id], map: "login_log_user_id_foreign")
}

model marketplace_leads {
  id                 Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid               String?   @unique(map: "marketplace_leads_uuid_unique") @db.Char(36)
  framework_id       Int       @db.UnsignedInt
  user_id            Int       @db.UnsignedInt
  solution_id        Int       @db.UnsignedInt
  subcontrol_id      Int?      @db.UnsignedInt
  category_id        Int?
  status             String?
  created_at         DateTime? @db.Timestamp(0)
  updated_at         DateTime? @db.Timestamp(0)
  uuid_framework_id  String?   @db.Char(36)
  uuid_user_id       String?   @db.Char(36)
  uuid_solution_id   String?   @db.Char(36)
  uuid_subcontrol_id String?   @db.Char(36)
  uuid_category_id   String?   @db.Char(36)
}

model message_queue {
  id              Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid            String?   @unique(map: "message_queue_uuid_unique") @db.Char(36)
  to_user_id      Int       @db.UnsignedInt
  type            Int       @db.TinyInt
  status          Int       @db.TinyInt
  retries         Int       @db.TinyInt
  created_at      DateTime? @db.Timestamp(0)
  updated_at      DateTime? @db.Timestamp(0)
  uuid_to_user_id String?   @db.Char(36)
  user            user      @relation(fields: [to_user_id], references: [id], onUpdate: Restrict, map: "message_queue_to_user_id_foreign")

  @@index([to_user_id], map: "message_queue_to_user_id_foreign")
}

model migrations {
  id        Int     @id @default(autoincrement()) @db.UnsignedInt
  uuid      String? @unique(map: "migrations_uuid_unique") @db.Char(36)
  migration String
  batch     Int
}

model nestjs_migrations {
  id        Int     @id @default(autoincrement())
  uuid      String? @unique(map: "nestjs_migrations_uuid_unique") @db.Char(36)
  timestamp BigInt
  name      String  @db.VarChar(255)
}

/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model notification_schedules {
  id                                            BigInt             @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                          String?            @unique(map: "notification_schedules_uuid_unique") @db.Char(36)
  type                                          String
  content                                       String?            @db.VarChar(8000)
  subject                                       String
  view_name                                     String
  reply_to                                      String?
  status                                        String             @default("P") @db.Char(1)
  scheduled_at                                  DateTime           @db.DateTime(0)
  delivered_at                                  DateTime?          @db.DateTime(0)
  attempt                                       Int                @default(0)
  receiver_id                                   Int?               @db.UnsignedInt
  receiver_name                                 String
  receiver_email                                String
  sender_id                                     Int?               @db.UnsignedInt
  sender_email                                  String?
  sender_name                                   String?
  created_at                                    DateTime?          @db.Timestamp(0)
  updated_at                                    DateTime?          @db.Timestamp(0)
  deleted_at                                    DateTime?          @db.Timestamp(0)
  subscription_type_id                          String?            @db.Char(36)
  notification_schedule_id                      Int?               @db.UnsignedInt
  uuid_receiver_id                              String?            @db.Char(36)
  uuid_sender_id                                String?            @db.Char(36)
  uuid_subscription_type_id                     String?            @db.Char(36)
  uuid_notification_schedule_id                 String?            @db.Char(36)
  user_notification_schedules_receiver_idTouser user?              @relation("notification_schedules_receiver_idTouser", fields: [receiver_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "notification_schedules_receiver_id_foreign")
  user_notification_schedules_sender_idTouser   user?              @relation("notification_schedules_sender_idTouser", fields: [sender_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "notification_schedules_sender_id_foreign")
  subscription_type                             subscription_type? @relation(fields: [subscription_type_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "notification_schedules_subscription_type_id_foreign")

  @@index([receiver_id], map: "notification_schedules_receiver_id_foreign")
  @@index([sender_id], map: "notification_schedules_sender_id_foreign")
  @@index([subscription_type_id], map: "notification_schedules_subscription_type_id_foreign")
}

model organization {
  id                                Int                                 @id @default(autoincrement()) @db.UnsignedInt
  name                              String
  consultant_logo                   String?
  partner_logo                      String
  s3_bucket                         String                              @default("AWS_S3_BUCKET_STORAGE")
  s3_driver                         String                              @default("s3_storage")
  created_at                        DateTime?                           @db.Timestamp(0)
  updated_at                        DateTime?                           @db.Timestamp(0)
  partner_id                        String
  contact_first_name                String
  contact_last_name                 String
  email                             String?
  contact_number                    String?
  street                            String?
  city                              String?
  state                             String?
  zip                               String?
  country                           String?
  domain                            String
  product_id                        Int?                                @db.UnsignedInt
  product_type                      String
  unit_price                        Decimal?                            @db.Decimal(8, 2)
  invoiced                          String
  multi_year                        String?
  industry                          String?
  account_type                      String?
  partner_account_type              String?
  lead_source                       String?
  notes                             String?
  employees                         String?                             @default("0")
  ask_expert_id                     Int?                                @db.UnsignedInt
  status                            Boolean?
  current_program_id                Int?                                @db.UnsignedInt
  mfa_enabled                       Boolean?
  email_domain                      String?
  tenant_logo                       String?
  technology_environment_overview   String?                             @db.VarChar(2048)
  support_id                        Int?
  crm_id                            String?
  reporting_logo                    String?
  name_short                        String?
  uuid                              String                              @unique(map: "organization_uuid_unique") @db.Char(36)
  preferences                       Json?
  settings                          Json?
  sso_connection                    String?                             @db.VarChar(255)
  branding                          Json?
  allow_organization_theme_override Boolean?                            @default(false)
  uuid_partner_id                   String?                             @db.Char(36)
  uuid_product_id                   String?                             @db.Char(36)
  uuid_ask_expert_id                String?                             @db.Char(36)
  uuid_current_program_id           String?                             @db.Char(36)
  uuid_support_id                   String?                             @db.Char(36)
  uuid_crm_id                       String?                             @db.Char(36)
  storage_driver                    String                              @default("s3_driver")
  storage_root                      String?
  storage_folder                    String?
  storage_configuration             Json?
  connection_status                 String?
  api_key                           api_key[]
  app_template                      app_template[]
  assessment                        assessment[]
  assessment_template               assessment_template[]
  framework                         framework[]
  login_log                         login_log[]
  organization_bundle_map           organization_bundle_map[]           @ignore
  organization_connector            organization_connector[]
  organization_document_categories  organization_document_categories[]  @ignore
  organization_predesign_frameworks organization_predesign_frameworks[]
  organization_product_map          organization_product_map[]          @ignore
  organization_products             organization_products[]             @ignore
  organization_rule_map             organization_rule_map[]             @ignore
  organization_subscription         organization_subscription[]
  organization_user_map             organization_user_map[]             @ignore
  program                           program[]
  recurrence                        recurrence[]
  reports                           reports[]
  risk_field                        risk_field[]
  role                              role[]
  sub_control_template              sub_control_template[]
  subscription_history              subscription_history[]              @ignore
  task                              task[]
  task_tags                         task_tags[]
  template                          template[]
  user                              user[]
  user_org_program_map              user_org_program_map[]              @ignore
  vendor_types                      vendor_types[]
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model organization_bundle_map {
  organization_id      Int          @db.UnsignedInt
  bundle_id            String       @db.Char(36)
  uuid_organization_id String?      @db.Char(36)
  uuid_bundle_id       String?      @db.Char(36)
  organization         organization @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "new_organization_product_map_organization_id_foreign")

  @@index([bundle_id], map: "bundles")
  @@index([organization_id], map: "new_organization_product_map_organization_id_foreign")
  @@ignore
}

model organization_connector {
  id                   Int                                     @id @default(autoincrement()) @db.UnsignedInt
  uuid                 String                                  @db.Char(36)
  slug                 String
  connector_id         Int                                     @db.UnsignedInt
  organization_id      Int                                     @db.UnsignedInt
  oauth2_access_token  String                                  @db.Text
  oauth2_refresh_token String                                  @db.Text
  oauth2_expires       DateTime?                               @db.DateTime(0)
  update_frequency     organization_connector_update_frequency
  update_offset        Int
  update_time          DateTime                                @db.Time(0)
  is_recurring         Boolean                                 @default(false)
  status               String                                  @default("on")
  created_at           DateTime?                               @db.Timestamp(0)
  updated_at           DateTime?                               @db.Timestamp(0)
  deleted_at           DateTime?                               @db.Timestamp(0)
  provider_data        String                                  @db.Text
  uuid_connector_id    String?                                 @db.Char(36)
  uuid_organization_id String?                                 @db.Char(36)
  connectors           connectors                              @relation(fields: [connector_id], references: [id], onUpdate: Restrict, map: "organization_connector_connector_id_foreign")
  organization         organization                            @relation(fields: [organization_id], references: [id], onUpdate: Restrict, map: "organization_connector_organization_id_foreign")

  @@index([connector_id], map: "organization_connector_connector_id_foreign")
  @@index([organization_id], map: "organization_connector_organization_id_foreign")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model organization_document_categories {
  organization_id           Int                 @db.UnsignedInt
  document_category_id      String              @db.Char(36)
  created_at                DateTime?           @db.Timestamp(0)
  updated_at                DateTime?           @db.Timestamp(0)
  uuid_organization_id      String?             @db.Char(36)
  uuid_document_category_id String?             @db.Char(36)
  document_categories       document_categories @relation(fields: [document_category_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "organization_document_categories_document_category_id_foreign")
  organization              organization        @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "organization_document_categories_organization_id_foreign")

  @@index([document_category_id], map: "organization_document_categories_document_category_id_foreign")
  @@index([organization_id], map: "organization_document_categories_organization_id_foreign")
  @@ignore
}

model organization_entity_log {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid                String?   @unique(map: "organization_entity_log_uuid_unique") @db.Char(36)
  type                String    @db.VarChar(10)
  object_id           String    @db.VarChar(255)
  discriminator       String?   @db.VarChar(255)
  transaction_hash    String?   @db.VarChar(40)
  diffs               String?   @db.Text
  blame_id            String?   @db.VarChar(255)
  blame_user          String?   @db.VarChar(255)
  blame_user_fqdn     String?   @db.VarChar(255)
  blame_user_firewall String?   @db.VarChar(100)
  ip                  String?   @db.VarChar(45)
  created_at          DateTime? @db.Timestamp(0)
  updated_at          DateTime? @db.Timestamp(0)
  uuid_object_id      String?   @db.Char(36)
  uuid_blame_id       String?   @db.Char(36)
}

model organization_predesign_frameworks {
  id                   BigInt        @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                 String?       @unique(map: "organization_predesign_frameworks_uuid_unique") @db.Char(36)
  organization_id      Int?          @db.UnsignedInt
  framework_id         Int?          @db.UnsignedInt
  created_at           DateTime?     @db.Timestamp(0)
  updated_at           DateTime?     @db.Timestamp(0)
  uuid_organization_id String?       @db.Char(36)
  uuid_framework_id    String?       @db.Char(36)
  framework            framework?    @relation(fields: [framework_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "organization_predesign_frameworks_framework_id_foreign")
  organization         organization? @relation(fields: [organization_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "organization_predesign_frameworks_organization_id_foreign")

  @@index([framework_id], map: "organization_predesign_frameworks_framework_id_foreign")
  @@index([organization_id], map: "organization_predesign_frameworks_organization_id_foreign")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model organization_product_map {
  organization_id      Int          @db.UnsignedInt
  product_id           Int          @db.UnsignedInt
  uuid_organization_id String?      @db.Char(36)
  uuid_product_id      String?      @db.Char(36)
  organization         organization @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "organization_product_map_organization_id_foreign")
  product              product      @relation(fields: [product_id], references: [id], onUpdate: Restrict, map: "organization_product_map_product_id_foreign")

  @@index([organization_id], map: "organization_product_map_organization_id_foreign")
  @@index([product_id], map: "organization_product_map_product_id_foreign")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model organization_products {
  organization_id      Int          @db.UnsignedInt
  bundle_id            String       @db.Char(36)
  validity_range       Json?
  created_at           DateTime?    @db.Timestamp(0)
  updated_at           DateTime?    @db.Timestamp(0)
  uuid_organization_id String?      @db.Char(36)
  uuid_bundle_id       String?      @db.Char(36)
  bundles              bundles      @relation(fields: [bundle_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "organization_products_bundle_id_foreign")
  organization         organization @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "organization_products_organization_id_foreign")

  @@index([bundle_id], map: "organization_products_bundle_id_foreign")
  @@index([organization_id], map: "organization_products_organization_id_foreign")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model organization_rule_map {
  organization_id      Int          @db.UnsignedInt
  rule_id              String       @db.Char(36)
  uuid_organization_id String?      @db.Char(36)
  uuid_rule_id         String?      @db.Char(36)
  organization         organization @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "organization_rule_map_organization_id_foreign")
  rule                 rule         @relation(fields: [rule_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "organization_rule_map_rule_id_foreign")

  @@index([organization_id], map: "organization_rule_map_organization_id_foreign")
  @@index([rule_id], map: "organization_rule_map_rule_id_foreign")
  @@ignore
}

model organization_scoring_key {
  id                   Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid                 String?   @unique(map: "organization_scoring_key_uuid_unique") @db.Char(36)
  organization_id      Int
  user_id              Int?
  description          String    @db.Text
  order                Int
  created_at           DateTime? @db.Timestamp(0)
  updated_at           DateTime? @db.Timestamp(0)
  uuid_organization_id String?   @db.Char(36)
  uuid_user_id         String?   @db.Char(36)
}

model organization_sku {
  id                   Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid                 String?   @unique(map: "organization_sku_uuid_unique") @db.Char(36)
  organization_id      Int?      @db.UnsignedInt
  sku_id               Int?      @db.UnsignedInt
  start_date           DateTime? @db.Date
  end_date             DateTime? @db.Date
  created_at           DateTime? @db.Timestamp(0)
  updated_at           DateTime? @db.Timestamp(0)
  updated_by           String?   @db.VarChar(45)
  user_id              Int?
  uuid_organization_id String?   @db.Char(36)
  uuid_sku_id          String?   @db.Char(36)
  uuid_updated_by      String?   @db.Char(36)
  uuid_user_id         String?   @db.Char(36)
}

model organization_subscription {
  id                        String            @id @db.Char(36)
  uuid                      String?           @unique(map: "organization_subscription_uuid_unique") @db.Char(36)
  organization_id           Int               @db.UnsignedInt
  subscription_type_id      String            @db.Char(36)
  validity_range            Json?
  uuid_organization_id      String?           @db.Char(36)
  uuid_subscription_type_id String?           @db.Char(36)
  organization              organization      @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "organization_subscription_type_map_organization_id_foreign")
  subscription_type         subscription_type @relation(fields: [subscription_type_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "organization_subscription_type_map_subscription_type_id_foreign")

  @@index([organization_id], map: "organization_subscription_type_map_organization_id_foreign")
  @@index([subscription_type_id], map: "organization_subscription_type_map_subscription_type_id_foreign")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model organization_user_map {
  organization_id      Int          @db.UnsignedInt
  user_id              Int          @db.UnsignedInt
  role                 String
  uuid_organization_id String?      @db.Char(36)
  uuid_user_id         String?      @db.Char(36)
  organization         organization @relation(fields: [organization_id], references: [id], onUpdate: Restrict, map: "organization_user_map_organization_id_foreign")
  user                 user         @relation(fields: [user_id], references: [id], onUpdate: Restrict, map: "organization_user_map_user_id_foreign")

  @@index([organization_id], map: "organization_user_map_organization_id_foreign")
  @@index([user_id], map: "organization_user_map_user_id_foreign")
  @@ignore
}

model partner {
  id                           Int                    @id @default(autoincrement()) @db.UnsignedInt
  partner_name                 String?
  partner_admin_email          String?
  created_at                   DateTime?              @db.Timestamp(0)
  updated_at                   DateTime?              @db.Timestamp(0)
  partner_logo                 String?
  s3_bucket                    String                 @default("AWS_S3_BUCKET_STORAGE")
  s3_driver                    String                 @default("s3_storage")
  mfa_enabled                  Boolean?
  email_domain                 String?
  type                         String?
  price_book                   String?
  amount                       Decimal?               @db.Decimal(8, 2)
  term                         String?
  trial_length                 Int?
  sub_domain                   String?
  crm_id                       String?
  cyberxchange                 Boolean                @default(false)
  uuid                         String                 @unique(map: "partner_uuid_unique") @db.Char(36)
  default_bundle_id            String?
  default_subscription_id      String?                @db.Char(36)
  sso_connection               String?                @db.VarChar(255)
  external_provisioning_url    String?
  settings                     Json?
  is_internal                  Boolean                @default(false)
  initial_setup_complete       Boolean                @default(false)
  uuid_crm_id                  String?                @db.Char(36)
  uuid_default_bundle_id       String?                @db.Char(36)
  uuid_default_subscription_id String?                @db.Char(36)
  storage_driver               String                 @default("s3_driver")
  storage_root                 String?
  storage_folder               String?
  storage_configuration        Json?
  connection_status            String?
  api_key                      api_key[]
  assessment_template          assessment_template[]
  bundles                      bundles?               @relation(fields: [default_bundle_id], references: [id], onUpdate: Restrict, map: "partner_default_bundle_id_foreign")
  subscription_type            subscription_type?     @relation(fields: [default_subscription_id], references: [id], onUpdate: NoAction, map: "partner_default_subscription_id_foreign")
  partner_bundle_map           partner_bundle_map[]   @ignore
  partner_role                 partner_role[]
  partner_subscription         partner_subscription[]
  recurrence                   recurrence[]
  reports                      reports[]
  task                         task[]
  template                     template[]
  vendor_types                 vendor_types[]

  @@index([default_bundle_id], map: "partner_default_bundle_id_foreign")
  @@index([default_subscription_id], map: "partner_default_subscription_id_foreign")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model partner_bundle_map {
  partner_id      Int     @db.UnsignedInt
  bundle_id       String  @db.Char(36)
  uuid_partner_id String? @db.Char(36)
  uuid_bundle_id  String? @db.Char(36)
  bundles         bundles @relation(fields: [bundle_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "partner_bundle_map_bundle_id_foreign")
  partner         partner @relation(fields: [partner_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "partner_product_map_partner_id_foreign")

  @@index([bundle_id], map: "partner_bundle_map_bundle_id_foreign")
  @@index([partner_id], map: "partner_product_map_partner_id_foreign")
  @@ignore
}

model partner_entity_log {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid                String?   @unique(map: "partner_entity_log_uuid_unique") @db.Char(36)
  type                String    @db.VarChar(10)
  object_id           String    @db.VarChar(255)
  discriminator       String?   @db.VarChar(255)
  transaction_hash    String?   @db.VarChar(40)
  diffs               String?   @db.Text
  blame_id            String?   @db.VarChar(255)
  blame_user          String?   @db.VarChar(255)
  blame_user_fqdn     String?   @db.VarChar(255)
  blame_user_firewall String?   @db.VarChar(100)
  ip                  String?   @db.VarChar(45)
  created_at          DateTime? @db.Timestamp(0)
  updated_at          DateTime? @db.Timestamp(0)
  uuid_object_id      String?   @db.Char(36)
  uuid_blame_id       String?   @db.Char(36)
}

model partner_generated_notifications {
  id              Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid            String?   @unique(map: "partner_generated_notifications_uuid_unique") @db.Char(36)
  subject         String
  body            String    @db.Text
  user_id         Int
  type            String
  partner_id      Int
  created_at      DateTime? @db.Timestamp(0)
  updated_at      DateTime? @db.Timestamp(0)
  uuid_user_id    String?   @db.Char(36)
  uuid_partner_id String?   @db.Char(36)
}

model partner_generated_notifications_organizations {
  id                                     Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid                                   String?   @unique(map: "partner_generated_notifications_organizations_uuid_unique") @db.Char(36)
  partner_generated_notification_id      Int
  organization_id                        Int
  role_id                                Int
  created_at                             DateTime? @db.Timestamp(0)
  updated_at                             DateTime? @db.Timestamp(0)
  uuid_partner_generated_notification_id String?   @db.Char(36)
  uuid_organization_id                   String?   @db.Char(36)
  uuid_role_id                           String?   @db.Char(36)
}

model partner_permission {
  id                          Int                           @id @default(autoincrement()) @db.UnsignedInt
  uuid                        String?                       @unique(map: "partner_permission_uuid_unique") @db.Char(36)
  name                        String
  code                        String
  description                 String?
  deleted_at                  DateTime?                     @db.Timestamp(0)
  partner_role_permission_map partner_role_permission_map[] @ignore
}

model partner_role {
  id                          Int                           @id @default(autoincrement()) @db.UnsignedInt
  uuid                        String?                       @unique(map: "partner_role_uuid_unique") @db.Char(36)
  name                        String
  description                 String?
  partner_id                  Int?                          @db.UnsignedInt
  deleted_at                  DateTime?                     @db.Timestamp(0)
  uuid_partner_id             String?                       @db.Char(36)
  partner                     partner?                      @relation(fields: [partner_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "partner_role_partner_id_foreign")
  partner_role_permission_map partner_role_permission_map[] @ignore

  @@index([partner_id], map: "partner_role_partner_id_foreign")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model partner_role_permission_map {
  partner_role_id            Int                @db.UnsignedInt
  partner_permission_id      Int                @db.UnsignedInt
  uuid_partner_role_id       String?            @db.Char(36)
  uuid_partner_permission_id String?            @db.Char(36)
  partner_permission         partner_permission @relation(fields: [partner_permission_id], references: [id], onUpdate: Restrict, map: "partner_role_permission_map_partner_permission_id_foreign")
  partner_role               partner_role       @relation(fields: [partner_role_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "partner_role_permission_map_partner_role_id_foreign")

  @@index([partner_permission_id], map: "partner_role_permission_map_partner_permission_id_foreign")
  @@index([partner_role_id], map: "partner_role_permission_map_partner_role_id_foreign")
  @@ignore
}

model partner_scoring_key {
  id              Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid            String?   @unique(map: "partner_scoring_key_uuid_unique") @db.Char(36)
  partner_id      Int
  user_id         Int
  description     String    @db.Text
  order           Int
  created_at      DateTime? @db.Timestamp(0)
  updated_at      DateTime? @db.Timestamp(0)
  uuid_partner_id String?   @db.Char(36)
  uuid_user_id    String?   @db.Char(36)
}

model partner_sku {
  id              Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid            String?   @unique(map: "partner_sku_uuid_unique") @db.Char(36)
  partner_id      Int?      @db.UnsignedInt
  sku_id          Int?      @db.UnsignedInt
  start_date      DateTime? @db.Date
  end_date        DateTime? @db.Date
  created_at      DateTime? @db.Timestamp(0)
  updated_at      DateTime? @db.Timestamp(0)
  user_id         Int?
  uuid_partner_id String?   @db.Char(36)
  uuid_sku_id     String?   @db.Char(36)
  uuid_user_id    String?   @db.Char(36)
}

model partner_subscription {
  id                        String            @id @db.Char(36)
  uuid                      String?           @unique(map: "partner_subscription_uuid_unique") @db.Char(36)
  partner_id                Int               @db.UnsignedInt
  subscription_type_id      String            @db.Char(36)
  validity_range            Json?
  uuid_partner_id           String?           @db.Char(36)
  uuid_subscription_type_id String?           @db.Char(36)
  partner                   partner           @relation(fields: [partner_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "partner_subscription_partner_id_foreign")
  subscription_type         subscription_type @relation(fields: [subscription_type_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "partner_subscription_subscription_type_id_foreign")

  @@index([partner_id], map: "partner_subscription_partner_id_foreign")
  @@index([subscription_type_id], map: "partner_subscription_subscription_type_id_foreign")
}

model partner_theme {
  id                          Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid                        String?   @unique(map: "partner_theme_uuid_unique") @db.Char(36)
  partner_id                  Int
  slug                        String
  coming_soon_file            String?
  login_image                 String?
  login_logo                  String?
  login_favicon               String?
  login_logo_background_color String?   @db.Char(10)
  login_button_color          String?   @db.Char(10)
  title                       String?   @db.VarChar(100)
  login_image_click           String?   @db.VarChar(512)
  created_at                  DateTime? @db.Timestamp(0)
  updated_at                  DateTime? @db.Timestamp(0)
  branding                    Json?
  uuid_partner_id             String?   @db.Char(36)
}

model password_reset {
  id         Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid       String?   @unique(map: "password_reset_uuid_unique") @db.Char(36)
  email      String
  token      String
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)

  @@index([email], map: "password_reset_email_index")
}

model permission {
  id                               Int                   @id @default(autoincrement()) @db.UnsignedInt
  uuid                             String?               @unique(map: "permission_uuid_unique") @db.Char(36)
  name                             String
  code                             String
  deleted_at                       DateTime?             @db.Timestamp(0)
  active                           Boolean               @default(true)
  created_at                       DateTime?             @db.Timestamp(0)
  updated_at                       DateTime?             @db.Timestamp(0)
  created_by                       Int?                  @db.UnsignedInt
  updated_by                       Int?                  @db.UnsignedInt
  uuid_created_by                  String?               @db.Char(36)
  uuid_updated_by                  String?               @db.Char(36)
  user_permission_created_byTouser user?                 @relation("permission_created_byTouser", fields: [created_by], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "permission_created_by_foreign")
  user_permission_updated_byTouser user?                 @relation("permission_updated_byTouser", fields: [updated_by], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "permission_updated_by_foreign")
  role_permission_map              role_permission_map[] @ignore

  @@index([created_by], map: "permission_created_by_foreign")
  @@index([updated_by], map: "permission_updated_by_foreign")
}

model personal_access_tokens {
  id             BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  tokenable_type String
  tokenable_id   BigInt    @db.UnsignedBigInt
  name           String
  token          String    @unique(map: "personal_access_tokens_token_unique") @db.VarChar(64)
  abilities      String?   @db.Text
  last_used_at   DateTime? @db.Timestamp(0)
  created_at     DateTime? @db.Timestamp(0)
  updated_at     DateTime? @db.Timestamp(0)

  @@index([tokenable_type, tokenable_id], map: "personal_access_tokens_tokenable_type_tokenable_id_index")
}

model platform_documents {
  id         Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid       String?   @unique(map: "platform_documents_uuid_unique") @db.Char(36)
  hash       String
  name       String
  url        String
  s3_bucket  String    @default("AWS_S3_BUCKET_PLATFORM_DOCUMENT")
  s3_driver  String    @default("s3_platform_document")
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
}

model policy_template_mapping {
  id                                  BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  policy_template_id                  Int
  common_sub_control_template_id      Int
  created_at                          DateTime? @db.Timestamp(0)
  updated_at                          DateTime? @db.Timestamp(0)
  uuid_policy_template_id             String?   @db.Char(36)
  uuid_common_sub_control_template_id String?   @db.Char(36)
}

model policy_templates {
  id          BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid        String?   @unique(map: "policy_templates_uuid_unique") @db.Char(36)
  name        String
  description String    @db.Text
  url         String
  created_at  DateTime? @db.Timestamp(0)
  updated_at  DateTime? @db.Timestamp(0)
}

model pre_set_corrective_actions {
  id                              BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                            String?   @unique(map: "pre_set_corrective_actions_uuid_unique") @db.Char(36)
  type                            String?
  app_name                        String?
  sub_control                     String?
  category_identifier             String?
  description                     String?   @db.Text
  corrective_action_plan_language String?   @db.Text
  created_at                      DateTime? @db.Timestamp(0)
  updated_at                      DateTime? @db.Timestamp(0)
}

model pre_set_risk_profiles {
  id                                   BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                                 String?   @unique(map: "pre_set_risk_profiles_uuid_unique") @db.Char(36)
  assessment_template_question_id      Int?
  type                                 String?
  app                                  String?
  sub_control                          String?
  risk_rating                          String?
  risk_score                           String?
  impact                               String?
  impact_score                         String?
  likelihood                           String?
  likelihood_score                     String?
  created_at                           DateTime? @db.Timestamp(0)
  updated_at                           DateTime? @db.Timestamp(0)
  uuid_assessment_template_question_id String?   @db.Char(36)
}

model product {
  id                       Int                        @id @default(autoincrement()) @db.UnsignedInt
  uuid                     String?                    @unique(map: "product_uuid_unique") @db.Char(36)
  name                     String?
  partner_id               String?
  created_at               DateTime?                  @db.Timestamp(0)
  updated_at               DateTime?                  @db.Timestamp(0)
  uuid_partner_id          String?                    @db.Char(36)
  framework_product_map    framework_product_map[]    @ignore
  organization_product_map organization_product_map[] @ignore
}

model program {
  id                             Int                     @id @default(autoincrement()) @db.UnsignedInt
  uuid                           String?                 @unique(map: "program_uuid_unique") @db.Char(36)
  name                           String
  organization_id                Int                     @db.UnsignedInt
  framework_id                   Int?                    @db.UnsignedInt
  total_budget                   Decimal?                @db.Decimal(11, 2)
  total_hours                    Int?
  start_date                     DateTime                @db.Date
  end_date                       DateTime                @db.Date
  projected_audit_date           DateTime?               @db.Date
  has_shown_projected_audit_date Boolean                 @default(false)
  level                          String?
  progress                       Decimal?                @db.Decimal(8, 4)
  created_at                     DateTime?               @db.Timestamp(0)
  updated_at                     DateTime?               @db.Timestamp(0)
  deleted_at                     DateTime?               @db.Timestamp(0)
  verified_progress              Decimal?                @default(0.0000) @db.Decimal(8, 4)
  uuid_organization_id           String?                 @db.Char(36)
  uuid_framework_id              String?                 @db.Char(36)
  app                            app[]
  framework                      framework?              @relation(fields: [framework_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "program_framework_id_foreign")
  organization                   organization            @relation(fields: [organization_id], references: [id], onUpdate: Restrict, map: "program_organization_id_foreign")
  program_framework_map          program_framework_map[] @ignore
  user_org_program_map           user_org_program_map[]  @ignore

  @@index([framework_id], map: "program_framework_id_foreign")
  @@index([level], map: "program_level_index")
  @@index([organization_id], map: "program_organization_id_foreign")
  @@index([id, organization_id], map: "search")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model program_framework_map {
  program_id        Int       @db.UnsignedInt
  framework_id      Int       @db.UnsignedInt
  uuid_program_id   String?   @db.Char(36)
  uuid_framework_id String?   @db.Char(36)
  framework         framework @relation(fields: [framework_id], references: [id], onUpdate: Restrict, map: "program_framework_map_framework_id_foreign")
  program           program   @relation(fields: [program_id], references: [id], onUpdate: Restrict, map: "program_framework_map_program_id_foreign")

  @@index([framework_id], map: "program_framework_map_framework_id_foreign")
  @@index([program_id], map: "program_framework_map_program_id_foreign")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model raw_apps_import {
  name                 String? @db.Text
  description          String? @db.Text
  icon                 String? @db.Text
  app_template_id      Int?
  uuid                 String? @unique(map: "raw_apps_import_uuid_unique") @db.Char(36)
  uuid_app_template_id String? @db.Char(36)

  @@ignore
}

model raw_assessment_template_questions {
  id                           BigInt  @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                         String? @unique(map: "raw_assessment_template_questions_uuid_unique") @db.Char(36)
  question                     String? @db.Text
  help_text                    String? @db.VarChar(500)
  category_id                  Int?    @db.UnsignedInt
  subcategory_id               Int?    @db.UnsignedInt
  response_type                String?
  response_json                String? @db.Text
  sub_control_template_id      Int?
  question_id                  Int?
  category                     String? @db.VarChar(100)
  sub_control_name             String? @db.VarChar(100)
  uuid_category_id             String? @db.Char(36)
  uuid_subcategory_id          String? @db.Char(36)
  uuid_sub_control_template_id String? @db.Char(36)
  uuid_question_id             String? @db.Char(36)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model raw_beazer_import {
  subcontrol          Float?  @db.Float
  Control_Description String? @map("Control Description") @db.Text
  comments            String? @db.Text
  vendors             String? @db.Text
  Roadmap__Yr__       String? @map("Roadmap (Yr.)") @db.Text
  Project_Dates       String? @map("Project Dates") @db.Text
  beazer_comments     String? @db.Text
  Owner               String? @db.Text
  wait                String? @db.Text
  uuid                String? @unique(map: "raw_beazer_import_uuid_unique") @db.Char(36)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model raw_cis_assessment {
  framework_id                 Int?
  app_template_id              Int?
  Sub_Control_Id               Int?    @map("Sub-Control Id")
  question                     String? @db.Text
  Control                      String? @db.Text
  Sub_Control                  String? @map("Sub-Control") @db.Text
  sub_control_template_id      Int?
  question_id                  Int?
  uuid                         String? @unique(map: "raw_cis_assessment_uuid_unique") @db.Char(36)
  uuid_framework_id            String? @db.Char(36)
  uuid_app_template_id         String? @db.Char(36)
  uuid_sub_control_template_id String? @db.Char(36)
  uuid_question_id             String? @db.Char(36)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model raw_company_export {
  Company_ID                       Int?    @map("Company ID")
  Name                             String? @db.Text
  Apptega_Multi_Tenant_Environment String? @map("Apptega Multi-Tenant Environment") @db.Text
  Company_owner                    String? @map("Company owner") @db.Text
  Industry                         String? @db.Text
  Last_Login_Roll_Up               String? @map("Last Login Roll Up") @db.Text
  Company_Domain_Name              String? @map("Company Domain Name") @db.Text
  Associated_Company_ID            String? @map("Associated Company ID") @db.Text
  Associated_Company               String? @map("Associated Company") @db.Text
  organization_id                  Int?
  uuid                             String? @unique(map: "raw_company_export_uuid_unique") @db.Char(36)
  uuid_organization_id             String? @db.Char(36)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model raw_control_activities {
  control_activity   String? @db.Text
  description        String? @db.Text
  app                String? @db.Text
  subcontrol         String? @db.Text
  subcontrol_id      Int?
  uuid               String? @unique(map: "raw_control_activities_uuid_unique") @db.Char(36)
  uuid_subcontrol_id String? @db.Char(36)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model raw_narrative_updates {
  id        Int?
  uuid      String? @unique(map: "raw_narrative_updates_uuid_unique") @db.Char(36)
  name      String? @db.Text
  narrative String? @db.Text

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model raw_task_pack_2 {
  Framework                               String? @db.Text
  App                                     String? @db.Text
  Subcontrol                              String? @db.Text
  Task                                    String? @db.Text
  Note                                    String? @db.Text
  Assigned_To__must_be_a_User_in_Apptega_ String? @map("Assigned To (must be a User in Apptega)") @db.Text
  Due_Date                                String? @map("Due Date") @db.Text
  Create_Alert                            String? @map("Create Alert") @db.Text
  Status                                  String? @db.Text
  Priority                                String? @db.Text
  uuid                                    String? @unique(map: "raw_task_pack_2_uuid_unique") @db.Char(36)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model raw_user_logins_4_13 {
  Email        String? @db.Text
  Created_At   String? @map("Created At") @db.Text
  last_login   String? @db.Text
  user_id      Int?
  uuid         String? @unique(map: "raw_user_logins_4_13_uuid_unique") @db.Char(36)
  uuid_user_id String? @db.Char(36)

  @@ignore
}

model recurrence {
  id              String        @id @db.Char(36)
  dt_start        DateTime      @db.DateTime(0)
  dt_end          DateTime?     @db.DateTime(0)
  rrule           String        @db.VarChar(255)
  template_id     String        @db.Char(36)
  entity_type     String
  last_occurrence DateTime?     @db.DateTime(0)
  next_occurrence DateTime      @db.DateTime(0)
  organization_id Int?          @db.UnsignedInt
  partner_id      Int?          @db.UnsignedInt
  created_at      DateTime      @default(now()) @db.DateTime(0)
  updated_at      DateTime      @default(now()) @db.DateTime(0)
  status          Int           @db.TinyInt
  deleted_at      DateTime?     @db.DateTime(0)
  frequency       Int?          @db.TinyInt
  template        template      @relation(fields: [template_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "recurrence_ibfk_1")
  organization    organization? @relation(fields: [organization_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "recurrence_ibfk_2")
  partner         partner?      @relation(fields: [partner_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "recurrence_ibfk_3")
  task            task[]

  @@index([organization_id], map: "organization_id")
  @@index([partner_id], map: "partner_id")
  @@index([template_id], map: "template_id")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model report_app_map {
  reports_id      Int     @db.UnsignedInt
  app_id          Int     @db.UnsignedInt
  uuid_reports_id String? @db.Char(36)
  uuid_app_id     String? @db.Char(36)
  app             app     @relation(fields: [app_id], references: [id], onUpdate: Restrict, map: "report_app_map_app_id_foreign")
  reports         reports @relation(fields: [reports_id], references: [id], onUpdate: Restrict, map: "report_app_map_reports_id_foreign")

  @@index([app_id], map: "report_app_map_app_id_foreign")
  @@index([reports_id], map: "report_app_map_reports_id_foreign")
  @@ignore
}

model report_schedule {
  id                           BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                         String?   @unique(map: "report_schedule_uuid_unique") @db.Char(36)
  report_name                  String
  report_description           String?   @db.Text
  report_type                  String
  link                         String?   @db.VarChar(2048)
  s3_bucket                    String    @default("AWS_S3_BUCKET")
  s3_driver                    String    @default("s3")
  cover                        Int
  summary                      Int
  app                          Int
  tasks                        Int       @db.UnsignedInt
  subcontrols                  Int       @db.UnsignedInt
  control_activity             Int
  risk                         Int
  budget                       Int
  scoring_audit                Int
  scoring_key                  Int
  vendor                       Int
  report_format                String
  organization_id              Int?      @db.UnsignedInt
  program_id                   Int?
  partner_id                   Int?      @db.UnsignedInt
  user_id                      Int?
  executive_report_program_ids String?
  created_by                   Int       @db.UnsignedInt
  status                       String
  gaps                         Int?      @db.TinyInt
  frequency                    String?   @db.VarChar(20)
  frequency_day                String?   @db.VarChar(50)
  frequency_week_day           String?   @db.VarChar(50)
  frequency_date               Int?
  frequency_month              Int?
  start_date                   DateTime? @db.Date
  end_date                     DateTime? @db.Date
  time                         DateTime? @db.Time(0)
  email_to                     String    @default("me") @db.VarChar(20)
  is_risk_beta                 Int       @default(1)
  created_at                   DateTime? @db.Timestamp(0)
  updated_at                   DateTime? @db.Timestamp(0)
  last_sent_at                 DateTime? @db.Date
  options                      Json?
  uuid_organization_id         String?   @db.Char(36)
  uuid_program_id              String?   @db.Char(36)
  uuid_partner_id              String?   @db.Char(36)
  uuid_user_id                 String?   @db.Char(36)
  uuid_created_by              String?   @db.Char(36)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model report_schedule_app_map {
  report_schedule_id      Int     @db.UnsignedInt
  app_id                  Int     @db.UnsignedInt
  uuid_report_schedule_id String? @db.Char(36)
  uuid_app_id             String? @db.Char(36)

  @@ignore
}

model report_schedule_user_map {
  id                      BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  report_schedule_id      Int       @db.UnsignedInt
  user_id                 Int       @db.UnsignedInt
  created_at              DateTime? @db.Timestamp(0)
  updated_at              DateTime? @db.Timestamp(0)
  uuid_report_schedule_id String?   @db.Char(36)
  uuid_user_id            String?   @db.Char(36)
}

model report_templates {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid       String?   @unique(map: "report_templates_uuid_unique") @db.Char(36)
  name       String    @unique(map: "report_templates_name_unique")
  template   String    @db.Text
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
}

model reports {
  id                           Int              @id @unique(map: "reports_id_unique") @default(autoincrement()) @db.UnsignedInt
  uuid                         String?          @unique(map: "reports_uuid_unique") @db.Char(36)
  report_schedule_id           Int              @db.UnsignedInt
  report_name                  String
  report_description           String           @db.LongText
  report_type                  String
  link                         String?          @db.VarChar(2048)
  s3_bucket                    String           @default("AWS_S3_BUCKET")
  s3_driver                    String           @default("s3")
  cover                        Int
  summary                      Int
  app                          Int
  tasks                        Int              @db.UnsignedInt
  subcontrols                  Int              @db.UnsignedInt
  control_activity             Int
  risk                         Int
  budget                       Int
  scoring_audit                Int
  scoring_key                  Int
  vendor                       Int
  report_format                String
  organization_id              Int?             @db.UnsignedInt
  program_id                   Int?
  framework_id                 Int?
  partner_id                   Int?             @db.UnsignedInt
  executive_report_program_ids String?
  created_by                   Int              @db.UnsignedInt
  status                       String
  created_at                   DateTime?        @db.Timestamp(0)
  updated_at                   DateTime?        @db.Timestamp(0)
  deleted_at                   DateTime?        @db.Timestamp(0)
  gaps                         Boolean?
  sent_email                   Boolean?
  vendor_risks                 Int
  options                      Json?
  uuid_report_schedule_id      String?          @db.Char(36)
  uuid_organization_id         String?          @db.Char(36)
  uuid_program_id              String?          @db.Char(36)
  uuid_framework_id            String?          @db.Char(36)
  uuid_partner_id              String?          @db.Char(36)
  uuid_created_by              String?          @db.Char(36)
  report_app_map               report_app_map[] @ignore
  user                         user             @relation(fields: [created_by], references: [id], onUpdate: Restrict, map: "reports_created_by_foreign")
  organization                 organization?    @relation(fields: [organization_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "reports_organization_id_foreign")
  partner                      partner?         @relation(fields: [partner_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "reports_partner_id_foreign")

  @@index([created_by], map: "reports_created_by_foreign")
  @@index([organization_id], map: "reports_organization_id_foreign")
  @@index([partner_id], map: "reports_partner_id_foreign")
}

model risk_field {
  id                     BigInt        @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                   String?       @unique(map: "risk_field_uuid_unique") @db.Char(36)
  field_name             String
  slug                   String?
  created_at             DateTime?     @db.Timestamp(0)
  updated_at             DateTime?     @db.Timestamp(0)
  organization_id        Int?          @db.UnsignedInt
  available_to_all_risks Boolean       @default(false)
  deleted_at             DateTime?     @db.Timestamp(0)
  organization           organization? @relation(fields: [organization_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "risk_field_organization_id_foreign")

  @@index([organization_id], map: "risk_field_organization_id_foreign")
}

model risk_level_scoring_history {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid                String?   @unique(map: "risk_level_scoring_history_uuid_unique") @db.Char(36)
  user_id             Int?      @db.UnsignedInt
  risk_rating_id      Int?      @db.UnsignedInt
  scored_by           String?
  old_value           Int?
  new_value           Int?
  created_at          DateTime? @db.Timestamp(0)
  updated_at          DateTime? @db.Timestamp(0)
  deleted_at          DateTime? @db.Timestamp(0)
  uuid_user_id        String?   @db.Char(36)
  uuid_risk_rating_id String?   @db.Char(36)
  uuid_scored_by      String?   @db.Char(36)
}

model risk_rating {
  id                     BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                   String?   @unique(map: "risk_rating_uuid_unique") @db.Char(36)
  org_id                 Int
  framework_id           String
  subcontrol_id          String?
  user_id                String
  title                  String
  description            String?   @db.Text
  likelihood             String?
  impact                 String?
  residual_impact        String?
  risk_type              String?
  response_type          String?
  status                 String?
  response_plan          String?   @db.LongText
  mitigation_controls    String?   @db.LongText
  notes                  String?   @db.LongText
  date_identified        String?
  residual_likelihood    String?
  response_due_date      String?
  key_risk               Boolean
  budget                 Decimal?  @db.Decimal(14, 2)
  annual_loss_expectancy Decimal?  @db.Decimal(14, 2)
  created_at             DateTime? @db.Timestamp(0)
  updated_at             DateTime? @db.Timestamp(0)
  deleted_at             DateTime? @db.Timestamp(0)
  uuid_org_id            String?   @db.Char(36)
  uuid_framework_id      String?   @db.Char(36)
  uuid_subcontrol_id     String?   @db.Char(36)
  uuid_user_id           String?   @db.Char(36)

  @@index([framework_id], map: "idx_framework_id")
  @@index([org_id], map: "idx_org_id")
  @@index([subcontrol_id], map: "idx_subcontrol_id")
  @@index([user_id], map: "idx_user_id")
}

model risk_rating_category {
  id                  BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                String?   @unique(map: "risk_rating_category_uuid_unique") @db.Char(36)
  risk_rating_id      Int
  org_id              Int
  name                String
  created_at          DateTime? @db.Timestamp(0)
  updated_at          DateTime? @db.Timestamp(0)
  uuid_risk_rating_id String?   @db.Char(36)
  uuid_org_id         String?   @db.Char(36)
}

model risk_rating_comments {
  id           BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid         String?   @unique(map: "risk_rating_comments_uuid_unique") @db.Char(36)
  risk_id      Int
  notes        String    @db.Text
  user_id      Int
  created_at   DateTime? @db.Timestamp(0)
  updated_at   DateTime? @db.Timestamp(0)
  uuid_risk_id String?   @db.Char(36)
  uuid_user_id String?   @db.Char(36)
}

model risk_rating_documents {
  id                  BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                String?   @unique(map: "risk_rating_documents_uuid_unique") @db.Char(36)
  risk_rating_id      Int       @db.UnsignedInt
  document_id         Int       @db.UnsignedInt
  created_at          DateTime? @db.Timestamp(0)
  updated_at          DateTime? @db.Timestamp(0)
  uuid_risk_rating_id String?   @db.Char(36)
  uuid_document_id    String?   @db.Char(36)
}

model risk_rating_field_map {
  id                   BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  organization_id      BigInt
  risk_rating_id       BigInt
  risk_field_id        BigInt
  value                String?
  created_at           DateTime? @db.Timestamp(0)
  updated_at           DateTime? @db.Timestamp(0)
  uuid_organization_id String?   @db.Char(36)
  uuid_risk_rating_id  String?   @db.Char(36)
  uuid_risk_field_id   String?   @db.Char(36)
  label                String?
  active               Boolean   @default(true)
}

model risk_rating_subcontrol_map {
  id                 BigInt  @id @default(autoincrement()) @db.UnsignedBigInt
  risk_id            Int     @db.UnsignedInt
  subcontrol_id      Int     @db.UnsignedInt
  level              String?
  uuid_risk_id       String? @db.Char(36)
  uuid_subcontrol_id String? @db.Char(36)

  @@index([risk_id], map: "idx_risk_rating")
  @@index([subcontrol_id], map: "idx_sub_control")
}

model risk_rating_user_map {
  id                  BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  risk_rating_id      Int
  user_id             Int
  created_at          DateTime? @db.Timestamp(0)
  updated_at          DateTime? @db.Timestamp(0)
  uuid_risk_rating_id String?   @db.Char(36)
  uuid_user_id        String?   @db.Char(36)

  @@index([risk_rating_id, user_id], map: "idx_risk_user")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model risk_ratings_import_raw {
  App_Name__Control_ String? @map("App Name (Control)") @db.Text
  Subcontrol         String? @db.Text
  plan               String? @db.Text
  Impact             String? @db.Text
  Likelihood         String? @db.Text
  subcontrol_id      Int?
  uuid               String? @unique(map: "risk_ratings_import_raw_uuid_unique") @db.Char(36)
  uuid_subcontrol_id String? @db.Char(36)

  @@ignore
}

model risk_vendor_map {
  id                          BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  risk_id                     Int
  vendor_id                   Int
  vendor_form_id              Int?
  form_instance_id            Int?
  form_field_id               Int?
  form_field_response_id      Int?
  created_at                  DateTime? @db.Timestamp(0)
  updated_at                  DateTime? @db.Timestamp(0)
  follow_up_number            Int       @default(0)
  uuid_risk_id                String?   @db.Char(36)
  uuid_vendor_id              String?   @db.Char(36)
  uuid_vendor_form_id         String?   @db.Char(36)
  uuid_form_instance_id       String?   @db.Char(36)
  uuid_form_field_id          String?   @db.Char(36)
  uuid_form_field_response_id String?   @db.Char(36)
}

model role {
  id                        Int                         @id @default(autoincrement()) @db.UnsignedInt
  uuid                      String?                     @unique(map: "role_uuid_unique") @db.Char(36)
  name                      String
  slug                      String?
  organization_id           Int?                        @db.UnsignedInt
  deleted_at                DateTime?                   @db.Timestamp(0)
  description               String?                     @db.VarChar(2048)
  assigned_only             Boolean?
  uuid_organization_id      String?                     @db.Char(36)
  organization              organization?               @relation(fields: [organization_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "role_organization_id_foreign")
  role_field_permission_map role_field_permission_map[] @ignore
  role_framework_map        role_framework_map[]        @ignore
  role_permission_map       role_permission_map[]       @ignore
  user                      user[]

  @@index([organization_id], map: "role_organization_id_foreign")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model role_field_permission_map {
  role_id                  Int              @db.UnsignedInt
  field_permission_id      Int              @db.UnsignedInt
  view                     Boolean?
  update                   Boolean?
  uuid_role_id             String?          @db.Char(36)
  uuid_field_permission_id String?          @db.Char(36)
  field_permission         field_permission @relation(fields: [field_permission_id], references: [id], onUpdate: Restrict, map: "role_field_permission_map_field_permission_id_foreign")
  role                     role             @relation(fields: [role_id], references: [id], onUpdate: Restrict, map: "role_field_permission_map_role_id_foreign")

  @@index([field_permission_id], map: "role_field_permission_map_field_permission_id_foreign")
  @@index([role_id], map: "role_field_permission_map_role_id_foreign")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model role_framework_map {
  role_id           Int       @db.UnsignedInt
  framework_id      Int       @db.UnsignedInt
  created_at        DateTime? @db.Timestamp(0)
  updated_at        DateTime? @db.Timestamp(0)
  uuid_role_id      String?   @db.Char(36)
  uuid_framework_id String?   @db.Char(36)
  framework         framework @relation(fields: [framework_id], references: [id], onUpdate: Restrict, map: "role_framework_map_framework_id_foreign")
  role              role      @relation(fields: [role_id], references: [id], onUpdate: Restrict, map: "role_framework_map_role_id_foreign")

  @@index([framework_id], map: "role_framework_map_framework_id_foreign")
  @@index([role_id], map: "role_framework_map_role_id_foreign")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model role_permission_map {
  role_id            Int        @db.UnsignedInt
  permission_id      Int        @db.UnsignedInt
  uuid_role_id       String?    @db.Char(36)
  uuid_permission_id String?    @db.Char(36)
  permission         permission @relation(fields: [permission_id], references: [id], onUpdate: Restrict, map: "role_permission_map_permission_id_foreign")
  role               role       @relation(fields: [role_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "role_permission_map_role_id_foreign")

  @@index([permission_id], map: "role_permission_map_permission_id_foreign")
  @@index([role_id], map: "role_permission_map_role_id_foreign")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model role_permission_map_3PAuditor_Test {
  role_id            Int     @db.UnsignedInt
  permission_id      Int     @db.UnsignedInt
  uuid_role_id       String? @db.Char(36)
  uuid_permission_id String? @db.Char(36)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rrsm {
  id                 BigInt    @default(0) @db.UnsignedBigInt
  uuid               String?   @unique(map: "rrsm_uuid_unique") @db.Char(36)
  risk_id            Int       @db.UnsignedInt
  subcontrol_id      Int       @db.UnsignedInt
  level              String?
  created_at         DateTime? @db.Timestamp(0)
  updated_at         DateTime? @db.Timestamp(0)
  uuid_risk_id       String?   @db.Char(36)
  uuid_subcontrol_id String?   @db.Char(36)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model rrsm_backup {
  id                 BigInt    @default(0) @db.UnsignedBigInt
  uuid               String?   @unique(map: "rrsm_backup_uuid_unique") @db.Char(36)
  risk_id            Int       @db.UnsignedInt
  subcontrol_id      Int       @db.UnsignedInt
  level              String?
  created_at         DateTime? @db.Timestamp(0)
  updated_at         DateTime? @db.Timestamp(0)
  uuid_risk_id       String?   @db.Char(36)
  uuid_subcontrol_id String?   @db.Char(36)

  @@ignore
}

model rule {
  id                    String                  @id @db.Char(36)
  uuid                  String?                 @unique(map: "rule_uuid_unique") @db.Char(36)
  name                  String
  expression            String
  sequence              Int?
  created_at            DateTime?               @db.Timestamp(0)
  updated_at            DateTime?               @db.Timestamp(0)
  deleted_at            DateTime?               @db.Timestamp(0)
  bundle_rule_map       bundle_rule_map[]       @ignore
  organization_rule_map organization_rule_map[] @ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model s {
  subcontrol_id      Int     @default(0) @db.UnsignedInt
  uuid               String? @unique(map: "s_uuid_unique") @db.Char(36)
  uuid_subcontrol_id String? @db.Char(36)

  @@ignore
}

model scoring_history {
  id                  Int          @id @unique(map: "id_UNIQUE") @default(autoincrement()) @db.UnsignedInt
  uuid                String?      @unique(map: "scoring_history_uuid_unique") @db.Char(36)
  user_id             Int          @db.UnsignedInt
  sub_control_id      Int?         @db.UnsignedInt
  scored_by           String
  old_value           String
  new_value           String
  created_at          DateTime?    @db.Timestamp(0)
  updated_at          DateTime?    @db.Timestamp(0)
  deleted_at          DateTime?    @db.Timestamp(0)
  is_parent           Int          @default(0) @db.TinyInt
  uuid_user_id        String?      @db.Char(36)
  uuid_sub_control_id String?      @db.Char(36)
  uuid_scored_by      String?      @db.Char(36)
  sub_control         sub_control? @relation(fields: [sub_control_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_subcontrol")

  @@index([sub_control_id], map: "fk_subcontrol_idx")
  @@index([id, user_id, sub_control_id], map: "idx_complex")
  @@index([created_at], map: "idx_created_at")
  @@index([user_id], map: "idx_user")
}

model sku {
  id         Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid       String?   @unique(map: "sku_uuid_unique") @db.Char(36)
  name       String
  slug       String
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
}

model solution_exceptions {
  id                          BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                        String?   @unique(map: "solution_exceptions_uuid_unique") @db.Char(36)
  solution_id                 Int       @db.UnsignedInt
  framework_id                Int?
  subcontrol_template_id      Int?
  created_at                  DateTime? @db.Timestamp(0)
  updated_at                  DateTime? @db.Timestamp(0)
  uuid_solution_id            String?   @db.Char(36)
  uuid_framework_id           String?   @db.Char(36)
  uuid_subcontrol_template_id String?   @db.Char(36)
}

model solution_reviews {
  id               BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid             String?   @unique(map: "solution_reviews_uuid_unique") @db.Char(36)
  user_id          Int       @db.UnsignedInt
  solution_id      Int       @db.UnsignedInt
  rating           Int
  review           String    @db.Text
  created_at       DateTime? @db.Timestamp(0)
  updated_at       DateTime? @db.Timestamp(0)
  uuid_user_id     String?   @db.Char(36)
  uuid_solution_id String?   @db.Char(36)
}

model sso_partner_role_mapping {
  id                   BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  partner_id           Int
  partner_role_id      Int
  sso_role_name        String
  created_at           DateTime? @db.Timestamp(0)
  updated_at           DateTime? @db.Timestamp(0)
  uuid_partner_id      String?   @db.Char(36)
  uuid_partner_role_id String?   @db.Char(36)
}

model sso_role_mapping {
  id                   BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  organization_id      Int
  role_id              Int
  sso_role_name        String
  created_at           DateTime? @db.Timestamp(0)
  updated_at           DateTime? @db.Timestamp(0)
  uuid_organization_id String?   @db.Char(36)
  uuid_role_id         String?   @db.Char(36)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model staging_app_subcontrols {
  app_name                     String? @db.Text
  app_icon                     String? @db.Text
  app_description              String? @db.Text
  sub_name                     String? @db.Text
  sub_description              String? @db.Text
  sub_guidance                 String? @db.Text
  sub_control_template_id      String? @db.Text
  parent_id                    String? @db.Text
  app_template_id              Int?
  parent_sub_name              String? @db.Text
  narrative                    String? @db.Text
  sub_order                    String? @db.Text
  uuid                         String? @unique(map: "staging_app_subcontrols_uuid_unique") @db.Char(36)
  uuid_sub_control_template_id String? @db.Char(36)
  uuid_parent_id               String? @db.Char(36)
  uuid_app_template_id         String? @db.Char(36)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model staging_sub_control_map {
  sub_control_id           Int?
  copy_sub_control_id      Int?
  uuid_sub_control_id      String? @db.Char(36)
  uuid_copy_sub_control_id String? @db.Char(36)

  @@ignore
}

model sub_control {
  id                           Int                        @id @default(autoincrement()) @db.UnsignedInt
  uuid                         String?                    @unique(map: "sub_control_uuid_unique") @db.Char(36)
  app_id                       Int                        @db.UnsignedInt
  parent_sub_control_id        Int?                       @db.UnsignedInt
  sub_control_template_id      Int?                       @db.UnsignedInt
  title                        String                     @db.VarChar(500)
  description                  String?                    @db.Text
  additional_guidance          String?                    @db.Text
  budget_amount                Decimal?                   @db.Decimal(14, 2)
  track_hours                  Int?
  notes                        String?                    @db.Text
  policy_section               String?                    @db.VarChar(2048)
  internal_control_number      String?                    @db.VarChar(2048)
  recommendations              String?                    @db.Text
  progress                     Decimal                    @db.Decimal(11, 2)
  vendor                       String?                    @db.VarChar(2048)
  document_link                String?                    @db.VarChar(2048)
  is_recurring_initiative      Boolean                    @default(false)
  recurring_period             String?                    @db.VarChar(2048)
  occurance_limit              Int?                       @db.UnsignedInt
  start_date                   DateTime?                  @db.Date
  end_date                     DateTime?                  @db.Date
  created_at                   DateTime?                  @db.Timestamp(0)
  updated_at                   DateTime?                  @db.Timestamp(0)
  deleted_at                   DateTime?                  @db.Timestamp(0)
  create_alert                 Boolean                    @default(true)
  is_disable                   Boolean                    @default(false)
  is_disable_note              String?
  level                        String?                    @db.VarChar(20)
  process_maturity_level       String?
  practice_status              String?
  verified                     sub_control_verified?      @default(dbgenerated("0"))
  implementation_detail        String?                    @db.Text
  score_weight                 Decimal?                   @db.Decimal(10, 2)
  identifier                   String?                    @db.VarChar(20)
  manually_scored              Boolean?                   @default(false)
  uuid_app_id                  String?                    @db.Char(36)
  uuid_parent_sub_control_id   String?                    @db.Char(36)
  uuid_sub_control_template_id String?                    @db.Char(36)
  objective_criteria_title     String?
  connector_subcontrols        connector_subcontrols[]
  scoring_history              scoring_history[]
  app                          app                        @relation(fields: [app_id], references: [id], onUpdate: Restrict, map: "sub_control_app_id_foreign")
  sub_control                  sub_control?               @relation("sub_controlTosub_control", fields: [parent_sub_control_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "sub_control_parent_sub_control_id_foreign")
  other_sub_control            sub_control[]              @relation("sub_controlTosub_control")
  sub_control_template         sub_control_template?      @relation(fields: [sub_control_template_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "sub_control_sub_control_template_id_foreign")
  sub_control_notes_map        sub_control_notes_map[]
  subcontrol_document_link     subcontrol_document_link[]
  subcontrol_profile           subcontrol_profile[]

  @@index([app_id], map: "sub_control_app_id_foreign")
  @@index([parent_sub_control_id], map: "sub_control_parent_sub_control_id_foreign")
  @@index([id, deleted_at, title(length: 191), description(length: 191)], map: "sub_control_search")
  @@index([sub_control_template_id], map: "sub_control_sub_control_template_id_foreign")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model sub_control_activity_map {
  sub_control_id           Int     @db.UnsignedInt
  control_activity_id      Int     @db.UnsignedInt
  level                    String?
  uuid_sub_control_id      String? @db.Char(36)
  uuid_control_activity_id String? @db.Char(36)

  @@index([control_activity_id], map: "idx_control_activity")
  @@index([sub_control_id], map: "sub_control_id")
  @@ignore
}

model sub_control_comments {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid                String?   @unique(map: "sub_control_comments_uuid_unique") @db.Char(36)
  sub_control_id      Int       @db.UnsignedInt
  notes               String?   @db.Text
  user_id             Int       @db.UnsignedInt
  created_at          DateTime? @db.Timestamp(0)
  updated_at          DateTime? @db.Timestamp(0)
  uuid_sub_control_id String?   @db.Char(36)
  uuid_user_id        String?   @db.Char(36)

  @@index([sub_control_id], map: "subcontrol_idx")
  @@index([user_id], map: "user_id_idx")
}

model sub_control_notes {
  id                    BigInt                  @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                  String?                 @db.Char(36)
  uuid_user_id          String?                 @db.Char(36)
  user_id               Int                     @db.UnsignedInt
  notes                 String                  @db.Text
  created_at            DateTime?               @db.Timestamp(0)
  updated_at            DateTime?               @db.Timestamp(0)
  user                  user                    @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "sub_control_notes_user_id_foreign")
  sub_control_notes_map sub_control_notes_map[]

  @@index([user_id], map: "sub_control_notes_user_id_foreign")
}

model sub_control_notes_map {
  id                  BigInt            @id @default(autoincrement()) @db.UnsignedBigInt
  sub_control_note_id BigInt            @db.UnsignedBigInt
  sub_control_id      Int               @db.UnsignedInt
  created_at          DateTime?         @db.Timestamp(0)
  updated_at          DateTime?         @db.Timestamp(0)
  sub_control         sub_control       @relation(fields: [sub_control_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "sub_control_notes_map_sub_control_id_foreign")
  sub_control_notes   sub_control_notes @relation(fields: [sub_control_note_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "sub_control_notes_map_sub_control_note_id_foreign")

  @@index([sub_control_id], map: "sub_control_notes_map_sub_control_id_foreign")
  @@index([sub_control_note_id], map: "sub_control_notes_map_sub_control_note_id_foreign")
}

model sub_control_progress_solution_map {
  id                  BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  solution_id         BigInt    @db.UnsignedBigInt
  sub_control_id      BigInt    @db.UnsignedBigInt
  created_at          DateTime? @db.Timestamp(0)
  updated_at          DateTime? @db.Timestamp(0)
  deleted_at          DateTime? @db.Timestamp(0)
  uuid_solution_id    String?   @db.Char(36)
  uuid_sub_control_id String?   @db.Char(36)

  @@index([solution_id], map: "solution_id")
  @@index([sub_control_id], map: "sub_control_id")
}

model sub_control_template {
  id                                                                                                    Int                                @id @default(autoincrement()) @db.UnsignedInt
  app_template_id                                                                                       Int?                               @db.UnsignedInt
  name                                                                                                  String                             @db.VarChar(500)
  description                                                                                           String                             @db.Text
  narrative                                                                                             String?                            @db.Text
  additional_guidance                                                                                   String?                            @db.Text
  order                                                                                                 Int                                @db.UnsignedInt
  icon                                                                                                  String
  organization_id                                                                                       Int?                               @db.UnsignedInt
  created_at                                                                                            DateTime?                          @db.Timestamp(0)
  updated_at                                                                                            DateTime?                          @db.Timestamp(0)
  deleted_at                                                                                            DateTime?                          @db.Timestamp(0)
  partner_id                                                                                            Int?                               @db.UnsignedInt
  parent_id                                                                                             Int?                               @db.UnsignedInt
  level                                                                                                 String?                            @db.VarChar(20)
  show_parent                                                                                           Boolean                            @default(false)
  uuid                                                                                                  String?                            @db.Char(36)
  identifier                                                                                            String?                            @db.VarChar(20)
  score_weight                                                                                          Decimal?                           @db.Decimal(10, 2)
  uuid_app_template_id                                                                                  String?                            @db.Char(36)
  uuid_organization_id                                                                                  String?                            @db.Char(36)
  uuid_partner_id                                                                                       String?                            @db.Char(36)
  uuid_parent_id                                                                                        String?                            @db.Char(36)
  objective_criteria_title                                                                              String?
  insurance_product_subcontrol_map                                                                      insurance_product_subcontrol_map[]
  sub_control                                                                                           sub_control[]
  app_template                                                                                          app_template?                      @relation(fields: [app_template_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "sub_control_template_app_template_id_foreign")
  organization                                                                                          organization?                      @relation(fields: [organization_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "sub_control_template_organization_id_foreign")
  sub_control_template_map_sub_control_template_map_child_sub_control_template_idTosub_control_template sub_control_template_map[]         @relation("sub_control_template_map_child_sub_control_template_idTosub_control_template")
  sub_control_template_map_sub_control_template_map_sub_control_template_idTosub_control_template       sub_control_template_map[]         @relation("sub_control_template_map_sub_control_template_idTosub_control_template")

  @@index([app_template_id], map: "sub_control_template_app_template_id_foreign")
  @@index([organization_id], map: "sub_control_template_organization_id_foreign")
}

model sub_control_template_levels {
  id                           Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid                         String?   @unique(map: "sub_control_template_levels_uuid_unique") @db.Char(36)
  sub_control_template_id      Int
  framework_id                 Int?
  level                        String
  created_at                   DateTime? @db.Timestamp(0)
  updated_at                   DateTime? @db.Timestamp(0)
  uuid_sub_control_template_id String?   @db.Char(36)
  uuid_framework_id            String?   @db.Char(36)

  @@index([framework_id])
  @@index([level], map: "sub_control_template_levels_level_index")
  @@index([sub_control_template_id], map: "sub_control_template_levels_sub_control_template_id_unique")
}

model sub_control_template_map {
  id                                                                                                Int                  @id @default(autoincrement()) @db.UnsignedInt
  sub_control_template_id                                                                           Int                  @db.UnsignedInt
  child_sub_control_template_id                                                                     Int                  @db.UnsignedInt
  created_at                                                                                        DateTime?            @db.Timestamp(0)
  updated_at                                                                                        DateTime?            @db.Timestamp(0)
  uuid_sub_control_template_id                                                                      String?              @db.Char(36)
  uuid_child_sub_control_template_id                                                                String?              @db.Char(36)
  sub_control_template_sub_control_template_map_child_sub_control_template_idTosub_control_template sub_control_template @relation("sub_control_template_map_child_sub_control_template_idTosub_control_template", fields: [child_sub_control_template_id], references: [id], onUpdate: Restrict, map: "sub_control_template_map_child_sub_control_template_id_foreign")
  sub_control_template_sub_control_template_map_sub_control_template_idTosub_control_template       sub_control_template @relation("sub_control_template_map_sub_control_template_idTosub_control_template", fields: [sub_control_template_id], references: [id], onUpdate: Restrict, map: "sub_control_template_map_sub_control_template_id_foreign")

  @@index([child_sub_control_template_id], map: "sub_control_template_map_child_sub_control_template_id_foreign")
  @@index([sub_control_template_id], map: "sub_control_template_map_sub_control_template_id_foreign")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model sub_control_user_map {
  sub_control_id      Int     @db.UnsignedInt
  user_id             Int     @db.UnsignedInt
  assigned_by         Int?    @db.UnsignedInt
  notified            Int
  uuid_sub_control_id String? @db.Char(36)
  uuid_user_id        String? @db.Char(36)
  uuid_assigned_by    String? @db.Char(36)

  @@index([sub_control_id], map: "sub_control_id")
  @@index([assigned_by], map: "sub_control_user_map_assigned_by_foreign")
  @@ignore
}

model subcontrol_connector_evidence_mapping {
  id                    BigInt                                                  @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                  String                                                  @db.Char(36)
  organization_id       Int                                                     @db.UnsignedInt
  subcontrol_id         Int                                                     @db.UnsignedInt
  evidence_item_id      String
  connector_id          Int                                                     @db.UnsignedInt
  update_frequency      subcontrol_connector_evidence_mapping_update_frequency?
  reccur_day            Int?                                                    @db.UnsignedInt
  reccur_week           String?
  reccur_month          String?
  update_offset         Int
  update_time           DateTime                                                @db.Time(0)
  is_recurring          Boolean                                                 @default(false)
  deleted_at            DateTime?                                               @db.Timestamp(0)
  created_at            DateTime?                                               @db.Timestamp(0)
  updated_at            DateTime?                                               @db.Timestamp(0)
  last_synced_at        DateTime?                                               @db.DateTime(0)
  uuid_organization_id  String?                                                 @db.Char(36)
  uuid_subcontrol_id    String?                                                 @db.Char(36)
  uuid_evidence_item_id String?                                                 @db.Char(36)
  uuid_connector_id     String?                                                 @db.Char(36)

  @@index([connector_id], map: "connector_id")
  @@index([organization_id], map: "organization_id")
  @@index([subcontrol_id], map: "subcontrol_id")
}

model subcontrol_document_link {
  id                   Int          @id @default(autoincrement()) @db.UnsignedInt
  uuid                 String?      @unique(map: "subcontrol_document_link_uuid_unique") @db.Char(36)
  subcontrol_id        Int?         @db.UnsignedInt
  organization_id      Int          @db.UnsignedInt
  document_link        String       @db.VarChar(2048)
  document_label       String?
  created_at           DateTime?    @db.Timestamp(0)
  updated_at           DateTime?    @db.Timestamp(0)
  uuid_subcontrol_id   String?      @db.Char(36)
  uuid_organization_id String?      @db.Char(36)
  sub_control          sub_control? @relation(fields: [subcontrol_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "subcontrol_document_link_subcontrol_id_foreign")

  @@index([subcontrol_id, document_link(length: 191)])
  @@index([subcontrol_id], map: "subcontrol_document_link_subcontrol_id_foreign")
}

model subcontrol_marketplace_mappings {
  id                                  Int       @id @default(autoincrement()) @db.UnsignedInt
  common_sub_control_template_id      Int
  marketplace_category_id             Int
  created_at                          DateTime? @db.Timestamp(0)
  updated_at                          DateTime? @db.Timestamp(0)
  uuid_common_sub_control_template_id String?   @db.Char(36)
  uuid_marketplace_category_id        String?   @db.Char(36)

  @@index([common_sub_control_template_id], map: "ix_template_id")
}

model subcontrol_migration {
  id                    Int     @unique(map: "id_UNIQUE") @default(autoincrement()) @db.UnsignedInt
  uuid                  String? @unique(map: "subcontrol_migration_uuid_unique") @db.Char(36)
  source_framework      String? @db.VarChar(45)
  source_title          String? @db.VarChar(255)
  destination_framework String? @db.VarChar(45)
  destination_title     String? @db.VarChar(255)
  isactive              Int?    @default(0)
}

model subcontrol_profile {
  id                  Int         @id @default(autoincrement()) @db.UnsignedInt
  uuid                String?     @unique(map: "subcontrol_profile_uuid_unique") @db.Char(36)
  sub_control_id      Int         @db.UnsignedInt
  profile             String?     @db.Text
  action_steps        String?     @db.Text
  gaps                String?
  target_tier         String?
  current_tier        String?
  created_at          DateTime?   @db.Timestamp(0)
  updated_at          DateTime?   @db.Timestamp(0)
  deleted_at          DateTime?   @db.Timestamp(0)
  uuid_sub_control_id String?     @db.Char(36)
  sub_control         sub_control @relation(fields: [sub_control_id], references: [id], onUpdate: Restrict, map: "subcontrol_profile_sub_control_id_foreign")

  @@index([sub_control_id], map: "subcontrol_profile_sub_control_id_foreign")
}

model subcontrol_risk_rating {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid                String?   @unique(map: "subcontrol_risk_rating_uuid_unique") @db.Char(36)
  sub_control_id      Int       @db.UnsignedInt
  current_control     String?   @db.Text
  remediation_plan    String?   @db.Text
  likelyhood          String?
  impact              String?
  created_at          DateTime? @db.Timestamp(0)
  updated_at          DateTime? @db.Timestamp(0)
  deleted_at          DateTime? @db.Timestamp(0)
  uuid_sub_control_id String?   @db.Char(36)

  @@index([sub_control_id], map: "sub_control_id")
}

model subcontrol_vendor {
  id                 Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid               String?   @unique(map: "subcontrol_vendor_uuid_unique") @db.Char(36)
  subcontrol_id      Int       @db.UnsignedInt
  vendor_id          Int
  level              String?
  created_at         DateTime? @db.Timestamp(0)
  updated_at         DateTime? @db.Timestamp(0)
  uuid_subcontrol_id String?   @db.Char(36)
  uuid_vendor_id     String?   @db.Char(36)

  @@index([subcontrol_id], map: "subcontrol_id")
  @@index([vendor_id], map: "vendor_id")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model subscription_history {
  id                   String            @db.Char(36)
  uuid                 String?           @unique(map: "subscription_history_uuid_unique") @db.Char(36)
  organization_id      Int               @db.UnsignedInt
  subscription_id      String
  created_at           DateTime?         @db.Timestamp(0)
  updated_at           DateTime?         @db.Timestamp(0)
  uuid_organization_id String?           @db.Char(36)
  uuid_subscription_id String?           @db.Char(36)
  organization         organization      @relation(fields: [organization_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "subscription_history_organization_id_foreign")
  subscription_type    subscription_type @relation(fields: [subscription_id], references: [id], onDelete: Cascade, onUpdate: NoAction, map: "subscription_history_subscription_id_foreign")

  @@index([organization_id], map: "subscription_history_organization_id_foreign")
  @@index([subscription_id], map: "subscription_history_subscription_id_foreign")
  @@ignore
}

model subscription_type {
  id                           String                         @id @db.Char(36)
  uuid                         String?                        @unique(map: "subscription_type_uuid_unique") @db.Char(36)
  name                         String
  slug                         String
  parameters                   String?
  description                  String?
  help_text                    String?
  created_at                   DateTime?                      @db.Timestamp(0)
  updated_at                   DateTime?                      @db.Timestamp(0)
  deleted_at                   DateTime?                      @db.Timestamp(0)
  notification_schedules       notification_schedules[]
  organization_subscription    organization_subscription[]
  partner                      partner[]
  partner_subscription         partner_subscription[]
  subscription_history         subscription_history[]         @ignore
  subscription_type_bundle_map subscription_type_bundle_map[] @ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model subscription_type_bundle_map {
  subscription_type_id      String            @db.Char(36)
  bundle_id                 String            @db.Char(36)
  uuid_subscription_type_id String?           @db.Char(36)
  uuid_bundle_id            String?           @db.Char(36)
  bundles                   bundles           @relation(fields: [bundle_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "subscription_type_bundle_map_bundle_id_foreign")
  subscription_type         subscription_type @relation(fields: [subscription_type_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "subscription_type_bundle_map_subscription_type_id_foreign")

  @@index([bundle_id], map: "subscription_type_bundle_map_bundle_id_foreign")
  @@index([subscription_type_id], map: "subscription_type_bundle_map_subscription_type_id_foreign")
  @@ignore
}

model subscription_type_entity_log {
  id                  Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid                String?   @unique(map: "subscription_type_entity_log_uuid_unique") @db.Char(36)
  type                String    @db.VarChar(10)
  object_id           String    @db.VarChar(255)
  discriminator       String?   @db.VarChar(255)
  transaction_hash    String?   @db.VarChar(40)
  diffs               String?   @db.Text
  blame_id            String?   @db.VarChar(255)
  blame_user          String?   @db.VarChar(255)
  blame_user_fqdn     String?   @db.VarChar(255)
  blame_user_firewall String?   @db.VarChar(100)
  ip                  String?   @db.VarChar(45)
  created_at          DateTime? @db.Timestamp(0)
  updated_at          DateTime? @db.Timestamp(0)
  uuid_object_id      String?   @db.Char(36)
  uuid_blame_id       String?   @db.Char(36)
}

model system_setting {
  id            BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid          String?   @unique(map: "system_setting_uuid_unique") @db.Char(36)
  name          String    @unique(map: "system_setting_name_unique")
  label         String?
  group         String?
  sort_order    Int?
  help_text     String?   @db.Text
  field_type    String?
  type          String?
  default_value String?
  options       String?   @db.Text
  created_at    DateTime? @db.Timestamp(0)
  updated_at    DateTime? @db.Timestamp(0)
}

model system_setting_value {
  id                     BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                   String?   @unique(map: "system_setting_value_uuid_unique") @db.Char(36)
  system_setting_id      Int       @db.UnsignedInt
  organization_id        Int       @db.UnsignedInt
  value                  String?
  created_at             DateTime? @db.Timestamp(0)
  updated_at             DateTime? @db.Timestamp(0)
  notify_time            DateTime  @default(dbgenerated("'08:00:00'")) @db.Time(0)
  uuid_system_setting_id String?   @db.Char(36)
  uuid_organization_id   String?   @db.Char(36)
  users                  String?   @db.Text
}

model task {
  id                       Int                        @id @unique(map: "id_UNIQUE") @default(autoincrement()) @db.UnsignedInt
  sub_control_id           Int                        @db.UnsignedInt
  level                    String?
  title                    String                     @db.VarChar(500)
  subject                  String?                    @db.VarChar(265)
  status                   Boolean
  priority                 String?
  track_hours              Int?
  create_alert             Boolean                    @default(false)
  due_date                 DateTime                   @db.Date
  order                    Int?
  created_at               DateTime?                  @db.Timestamp(0)
  updated_at               DateTime?                  @db.Timestamp(0)
  deleted_at               DateTime?                  @db.Timestamp(0)
  is_recurring_initiative  Boolean                    @default(false)
  recurring_period         String?                    @db.VarChar(2048)
  occurance_limit          Int?                       @db.UnsignedInt
  reccur_pattern           String?
  reccur_day               Int?                       @db.UnsignedInt
  reccur_week              String?
  reccur_month             String?
  reccur_number_of_months  Int?                       @db.UnsignedInt
  source_of_deficiency     String?                    @db.Text
  is_audit                 Boolean?
  objective_evidence       String?                    @db.Text
  audit_id                 Int?                       @db.UnsignedInt
  audit_request_id         Int?
  exceptionFlag            String?                    @db.VarChar(5)
  exceptionNote            String?                    @db.Text
  raw_task_id              Int?
  custom_1                 String?                    @db.VarChar(100)
  custom_2                 String?                    @db.VarChar(100)
  custom_3                 String?                    @db.VarChar(100)
  subcontrol_note          String?                    @db.Text
  reccur_week_day          String?
  reccur_number_week       String?
  uuid                     String?                    @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  type                     Int?
  organization_id          Int?                       @db.UnsignedInt
  partner_id               Int?                       @db.UnsignedInt
  uuid_sub_control_id      String?                    @db.Char(36)
  uuid_audit_id            String?                    @db.Char(36)
  uuid_audit_request_id    String?                    @db.Char(36)
  uuid_raw_task_id         String?                    @db.Char(36)
  uuid_organization_id     String?                    @db.Char(36)
  uuid_partner_id          String?                    @db.Char(36)
  recurrence_id            String?                    @db.Char(36)
  occurrence_position      Int?                       @db.UnsignedInt
  poam                     Boolean                    @default(true)
  audit_task_comment_users audit_task_comment_users[]
  document_tasks           document_tasks[]
  audit                    audit?                     @relation(fields: [audit_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_task_audit")
  organization             organization?              @relation(fields: [organization_id], references: [id], onUpdate: NoAction, map: "task_organization_id_foreign")
  partner                  partner?                   @relation(fields: [partner_id], references: [id], onUpdate: NoAction, map: "task_partner_id_foreign")
  recurrence               recurrence?                @relation(fields: [recurrence_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "task_recurrence_id_foreign_idx")
  task_connector_mapping   task_connector_mapping[]
  task_history             task_history[]
  task_notes_map           task_notes_map[]
  task_occurrence          task_occurrence[]
  task_relationship_map    task_relationship_map[]
  task_tags                task_tags[]

  @@index([audit_id], map: "fk_task_audit_idx")
  @@index([organization_id], map: "task_organization_id_foreign")
  @@index([partner_id], map: "task_partner_id_foreign")
  @@index([recurrence_id], map: "task_recurrence_id_foreign_idx")
  @@index([id, sub_control_id, subject, title, deleted_at], map: "task_search")
  @@index([sub_control_id], map: "task_sub_control_id")
}

model task_activities {
  id         BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid       String?   @unique(map: "task_activities_uuid_unique") @db.Char(36)
  activity   String
  slug       String
  created_at DateTime? @db.Timestamp(0)
  updated_at DateTime? @db.Timestamp(0)
}

model task_activity_user_map {
  id               BigInt   @id @default(autoincrement()) @db.UnsignedBigInt
  activity_id      Int
  task_id          Int
  user_id          Int
  action           String?
  new_value        String?
  old_value        String?
  performed_at     DateTime @db.DateTime(0)
  uuid_activity_id String?  @db.Char(36)
  uuid_task_id     String?  @db.Char(36)
  uuid_user_id     String?  @db.Char(36)
}

model task_comments {
  id                     Int                  @id @default(autoincrement()) @db.UnsignedInt
  uuid                   String?              @unique(map: "task_comments_uuid_unique") @db.Char(36)
  task_id                Int
  jira_comment_id        Int?
  notes                  String               @db.Text
  user_id                Int
  parent_comment_id      Int?
  comment_category       String?
  created_at             DateTime?            @db.Timestamp(0)
  updated_at             DateTime?            @db.Timestamp(0)
  uuid_task_id           String?              @db.Char(36)
  uuid_jira_comment_id   String?              @db.Char(36)
  uuid_user_id           String?              @db.Char(36)
  uuid_parent_comment_id String?              @db.Char(36)
  task_comments_read     task_comments_read[]
}

model task_comments_connector_mapping {
  id                   Int        @id @unique(map: "id_UNIQUE") @default(autoincrement()) @db.UnsignedInt
  task_comment_id      Int        @db.UnsignedInt
  jira_comment_id      Int?
  connector_id         Int        @db.UnsignedInt
  created_at           DateTime?  @db.Timestamp(0)
  updated_at           DateTime?  @db.Timestamp(0)
  uuid                 String?    @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  uuid_task_comment_id String?    @db.Char(36)
  uuid_jira_comment_id String?    @db.Char(36)
  uuid_connector_id    String?    @db.Char(36)
  connectors           connectors @relation(fields: [connector_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_task_comments_connector_mapping_user")

  @@index([connector_id], map: "fk_task_comments_connector_mapping_user_idx")
}

model task_comments_read {
  id              Int           @id @unique(map: "id_UNIQUE") @default(autoincrement()) @db.UnsignedInt
  comment_id      Int           @db.UnsignedInt
  user_id         Int           @db.UnsignedInt
  created_at      DateTime?     @db.Timestamp(0)
  updated_at      DateTime?     @db.Timestamp(0)
  uuid            String?       @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  uuid_comment_id String?       @db.Char(36)
  uuid_user_id    String?       @db.Char(36)
  task_comments   task_comments @relation(fields: [comment_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_task_comments_read_task_comments")
  user            user          @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_task_comments_read_user")

  @@index([comment_id], map: "fk_task_comments_read_task_comments_idx")
  @@index([user_id], map: "fk_task_comments_read_user_idx")
}

model task_connector_mapping {
  id                      Int              @id @unique(map: "id_UNIQUE") @default(autoincrement()) @db.UnsignedInt
  task_id                 Int              @db.UnsignedInt
  task_occurrence_id      Int?             @db.UnsignedInt
  connector_task_id       String
  user_connector_id       Int              @db.UnsignedInt
  created_at              DateTime?        @db.Timestamp(0)
  updated_at              DateTime?        @db.Timestamp(0)
  deleted_at              DateTime?        @db.Timestamp(0)
  uuid                    String?          @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  uuid_task_id            String?          @db.Char(36)
  uuid_task_occurrence_id String?          @db.Char(36)
  uuid_connector_task_id  String?          @db.Char(36)
  uuid_user_connector_id  String?          @db.Char(36)
  task                    task             @relation(fields: [task_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_task_connector_mapping_task")
  task_occurrence         task_occurrence? @relation(fields: [task_occurrence_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_task_connector_mapping_task_occurance")

  @@index([task_id], map: "fk_task_connector_mapping_task_idx")
  @@index([task_occurrence_id], map: "fk_task_connector_mapping_task_occurance_idx")
}

model task_connector_priority_mapping {
  id                         Int             @id @unique(map: "id_UNIQUE") @default(autoincrement()) @db.UnsignedInt
  user_connector_id          Int             @db.UnsignedInt
  task_priority              String
  connector_priority_id      Int?            @db.UnsignedInt
  created_at                 DateTime?       @db.Timestamp(0)
  updated_at                 DateTime?       @db.Timestamp(0)
  deleted_at                 DateTime?       @db.Timestamp(0)
  uuid                       String?         @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  uuid_user_connector_id     String?         @db.Char(36)
  uuid_connector_priority_id String?         @db.Char(36)
  user_connectors            user_connectors @relation(fields: [user_connector_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_connector_priority_mapping_user_connectors")

  @@index([user_connector_id], map: "fk_connector_priority_mapping_user_connectors_idx")
}

model task_connector_status_mapping {
  id                       Int             @id @unique(map: "id_UNIQUE") @default(autoincrement()) @db.UnsignedInt
  user_connector_id        Int             @db.UnsignedInt
  task_status              String
  connector_status_id      Int?            @db.UnsignedInt
  created_at               DateTime?       @db.Timestamp(0)
  updated_at               DateTime?       @db.Timestamp(0)
  deleted_at               DateTime?       @db.Timestamp(0)
  uuid                     String?         @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  uuid_user_connector_id   String?         @db.Char(36)
  uuid_connector_status_id String?         @db.Char(36)
  user_connectors          user_connectors @relation(fields: [user_connector_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_task_connector_status_mapping_user_connector")

  @@index([user_connector_id], map: "fk_task_connector_status_mapping_user_connector_idx")
}

model task_history {
  id           Int       @id @unique(map: "id_UNIQUE") @default(autoincrement()) @db.UnsignedInt
  task_id      Int       @db.UnsignedInt
  user_id      Int       @db.UnsignedInt
  status       String
  created_at   DateTime? @db.Timestamp(0)
  updated_at   DateTime? @db.Timestamp(0)
  uuid         String?   @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  uuid_task_id String?   @db.Char(36)
  uuid_user_id String?   @db.Char(36)
  task         task      @relation(fields: [task_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_task_history_task")
  user         user      @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_task_history_user")

  @@index([task_id], map: "fk_task_history_task_idx")
  @@index([user_id], map: "fk_task_history_user_idx")
}

model task_notes_map {
  id           Int       @id @unique(map: "id_UNIQUE") @default(autoincrement()) @db.UnsignedInt
  task_id      Int       @db.UnsignedInt
  note         String?   @db.Text
  uuid         String?   @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  created_at   DateTime? @db.Timestamp(0)
  updated_at   DateTime? @db.Timestamp(0)
  deleted_at   DateTime? @db.Timestamp(0)
  uuid_task_id String?   @db.Char(36)
  task         task      @relation(fields: [task_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "fk_task_notes_map_task")

  @@index([task_id], map: "fk_task_notes_map_task_idx")
  @@index([task_id, deleted_at])
  @@index([task_id])
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
/// This model or at least one of its fields has comments in the database, and requires an additional setup for migrations: Read more: https://pris.ly/d/database-comments
model task_notification_schedule {
  id           String    @db.Char(36)
  uuid         String?   @unique(map: "task_notification_schedule_uuid_unique") @db.Char(36)
  task_id      Int
  scheduled_at DateTime? @db.Timestamp(0)
  delivered_at DateTime? @db.Timestamp(0)
  status       Int?
  attempt      Int       @default(0)
  log_message  String
  created_at   DateTime? @db.Timestamp(0)
  updated_at   DateTime? @db.Timestamp(0)
  uuid_task_id String?   @db.Char(36)

  @@ignore
}

model task_occurrence {
  id                     Int                      @id @unique(map: "id_UNIQUE") @default(autoincrement()) @db.UnsignedInt
  task_id                Int                      @db.UnsignedInt
  occurrence             Int                      @db.UnsignedInt
  due_date               DateTime?                @db.Date
  status                 Boolean
  notified               Int                      @db.UnsignedInt
  created_at             DateTime?                @db.Timestamp(0)
  updated_at             DateTime?                @db.Timestamp(0)
  deleted_at             DateTime?                @db.Timestamp(0)
  uuid                   String?                  @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  uuid_task_id           String?                  @db.Char(36)
  task_connector_mapping task_connector_mapping[]
  task                   task                     @relation(fields: [task_id], references: [id], onUpdate: Restrict, map: "task_occurrence_task_id_foreign")

  @@index([id, task_id, due_date, notified], map: "occurrence")
  @@index([task_id], map: "task_occurrence_task_id_foreign")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model task_occurrence_backup_20210919 {
  id           Int       @default(0) @db.UnsignedInt
  uuid         String?   @unique(map: "task_occurrence_backup_20210919_uuid_unique") @db.Char(36)
  task_id      Int       @db.UnsignedInt
  occurrence   Int       @db.UnsignedInt
  due_date     DateTime? @db.Date
  status       Boolean
  notified     Int       @db.UnsignedInt
  created_at   DateTime? @db.Timestamp(0)
  updated_at   DateTime? @db.Timestamp(0)
  deleted_at   DateTime? @db.Timestamp(0)
  uuid_task_id String?   @db.Char(36)

  @@ignore
}

model task_occurrence_clone {
  id           Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid         String?   @unique(map: "task_occurrence_clone_uuid_unique") @db.Char(36)
  task_id      Int       @db.UnsignedInt
  occurrence   Int       @db.UnsignedInt
  due_date     DateTime? @db.Date
  status       Boolean
  notified     Int       @db.UnsignedInt
  created_at   DateTime? @db.Timestamp(0)
  updated_at   DateTime? @db.Timestamp(0)
  deleted_at   DateTime? @db.Timestamp(0)
  uuid_task_id String?   @db.Char(36)

  @@index([id, task_id, due_date, notified], map: "occurrence")
  @@index([task_id], map: "task_occurrence_task_id_foreign")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model task_owner_map {
  task_id          Int       @db.UnsignedInt
  owner_id         Int       @db.UnsignedInt
  assigned_by      Int?      @db.UnsignedInt
  notified         Int
  deleted_at       DateTime? @db.Timestamp(0)
  uuid_task_id     String?   @db.Char(36)
  uuid_owner_id    String?   @db.Char(36)
  uuid_assigned_by String?   @db.Char(36)

  @@index([assigned_by], map: "task_owner_map_assigned_by_foreign")
  @@index([owner_id], map: "task_owner_map_owner_id_foreign")
  @@index([task_id], map: "task_owner_map_task_id_foreign")
  @@ignore
}

model task_pack_raw {
  Framework                    String? @db.Text
  App                          String? @db.Text
  Subcontrol                   String? @db.Text
  Task_Subject                 String? @map("Task Subject") @db.Text
  Action_Items                 String? @map("Action Items") @db.Text
  assigned_to                  String? @db.Text
  due_date                     String? @db.Text
  create_alert                 String? @db.Text
  Status                       String? @db.Text
  Priority                     String? @db.Text
  task_id                      Int?
  user_id                      Int?
  id                           Int     @unique(map: "task_pack_raw_raw_task_id_idx") @default(autoincrement())
  uuid                         String? @unique(map: "task_pack_raw_uuid_unique") @db.Char(36)
  user_2_id                    Int?
  user_3_id                    Int?
  user_4_id                    Int?
  sub_control_template_id      Int?
  sub_control_id               Int?
  custom_1                     String? @db.Text
  custom_2                     String? @db.Text
  custom_3                     String? @db.Text
  sub_control_prefix           String? @db.Text
  title                        String? @db.Text
  tag1                         String? @db.Text
  uuid_task_id                 String? @db.Char(36)
  uuid_user_id                 String? @db.Char(36)
  uuid_user_2_id               String? @db.Char(36)
  uuid_user_3_id               String? @db.Char(36)
  uuid_user_4_id               String? @db.Char(36)
  uuid_sub_control_template_id String? @db.Char(36)
  uuid_sub_control_id          String? @db.Char(36)
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model task_packs_raw {
  subcontrol   String? @db.Text
  assigned_to  String? @db.Text
  subject      String? @db.Text
  status       String? @db.Text
  priority     String? @db.Text
  create_alert String? @db.Text
  due_date     String? @db.Text
  uuid         String? @unique(map: "task_packs_raw_uuid_unique") @db.Char(36)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model task_related_map {
  task_id              Int?      @db.UnsignedInt
  related_task_id      Int?      @db.UnsignedInt
  deleted_at           DateTime? @db.Timestamp(0)
  uuid_task_id         String?   @db.Char(36)
  uuid_related_task_id String?   @db.Char(36)

  @@index([task_id], map: "task_id")
  @@index([related_task_id], map: "task_related_map_related_task_id_foreign")
  @@ignore
}

model task_relationship_map {
  task_id         Int       @db.UnsignedInt
  uuid_task_id    String?   @db.VarChar(45)
  related_id      Int       @db.UnsignedInt
  uuid_related_id String?   @db.VarChar(45)
  related_type    String
  created_at      DateTime? @db.DateTime(0)
  updated_at      DateTime? @db.DateTime(0)
  deleted_at      DateTime? @db.DateTime(0)
  task            task      @relation(fields: [task_id], references: [id], onDelete: Cascade, map: "task_relationship_map_ibfk_1")

  @@unique([related_id, related_type, task_id], map: "unique_task_relationship")
  @@index([task_id], map: "task_id")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model task_status {
  id     Int?
  uuid   String? @unique(map: "task_status_uuid_unique") @db.Char(36)
  status String? @db.Text

  @@ignore
}

model task_tags {
  id                   BigInt       @id @unique(map: "id_UNIQUE") @default(autoincrement()) @db.UnsignedBigInt
  task_id              Int          @db.UnsignedInt
  organization_id      Int          @db.UnsignedInt
  name                 String?      @db.Text
  created_at           DateTime?    @db.Timestamp(0)
  updated_at           DateTime?    @db.Timestamp(0)
  uuid                 String?      @unique(map: "uuid_UNIQUE") @db.VarChar(45)
  uuid_task_id         String?      @db.Char(36)
  uuid_organization_id String?      @db.Char(36)
  organization         organization @relation(fields: [organization_id], references: [id], onUpdate: Restrict, map: "task_tags_organization_id_foreign")
  task                 task         @relation(fields: [task_id], references: [id], onUpdate: Restrict, map: "tasks_tags_task_id_foreign")

  @@index([organization_id], map: "task_tags_organization_id_foreign")
  @@index([task_id], map: "tasks_tags_task_id_foreign")
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model task_user_map {
  task_id          Int       @db.UnsignedInt
  user_id          Int       @db.UnsignedInt
  assigned_by      Int?      @db.UnsignedInt
  notified         Int
  deleted_at       DateTime? @db.Timestamp(0)
  created_at       DateTime? @db.Timestamp(0)
  updated_at       DateTime? @db.Timestamp(0)
  uuid_task_id     String?   @db.Char(36)
  uuid_user_id     String?   @db.Char(36)
  uuid_assigned_by String?   @db.Char(36)

  @@index([task_id], map: "task_id")
  @@index([assigned_by], map: "task_user_map_assigned_by_foreign")
  @@index([user_id], map: "user_id")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model temp_app {
  id                   Int?      @default(0) @db.UnsignedInt
  uuid                 String?   @unique(map: "temp_app_uuid_unique") @db.Char(36)
  program_id           Int       @db.UnsignedInt
  app_template_id      Int?      @db.UnsignedInt
  name                 String?
  description          String?   @db.VarChar(2048)
  status               Boolean?
  icon                 String?
  progress             Decimal?  @db.Decimal(8, 4)
  created_at           DateTime? @db.Timestamp(0)
  updated_at           DateTime? @db.Timestamp(0)
  deleted_at           DateTime? @db.Timestamp(0)
  uuid_program_id      String?   @db.Char(36)
  uuid_app_template_id String?   @db.Char(36)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model temp_delete_sub_control {
  id   Int     @default(0) @db.UnsignedInt
  uuid String? @unique(map: "temp_delete_sub_control_uuid_unique") @db.Char(36)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model temp_delete_sub_control_template {
  id   Int     @default(0) @db.UnsignedInt
  uuid String? @unique(map: "temp_delete_sub_control_template_uuid_unique") @db.Char(36)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model temp_scoring {
  id                  Int       @default(0) @db.UnsignedInt
  uuid                String?   @unique(map: "temp_scoring_uuid_unique") @db.Char(36)
  user_id             Int       @db.UnsignedInt
  sub_control_id      Int?      @db.UnsignedInt
  scored_by           String
  old_value           String
  new_value           String
  created_at          DateTime? @db.Timestamp(0)
  updated_at          DateTime? @db.Timestamp(0)
  deleted_at          DateTime? @db.Timestamp(0)
  uuid_user_id        String?   @db.Char(36)
  uuid_sub_control_id String?   @db.Char(36)
  uuid_scored_by      String?   @db.Char(36)

  @@index([created_at], map: "idx_created_at")
  @@index([id], map: "idx_id")
  @@index([sub_control_id], map: "idx_subcontrol")
  @@index([user_id], map: "idx_users")
  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model temp_sub_control {
  id                           Int       @default(0) @db.UnsignedInt
  uuid                         String?   @unique(map: "temp_sub_control_uuid_unique") @db.Char(36)
  app_id                       Int       @db.UnsignedInt
  parent_sub_control_id        Int?      @db.UnsignedInt
  sub_control_template_id      Int?      @db.UnsignedInt
  title                        String    @db.VarChar(261)
  description                  String?   @db.Text
  additional_guidance          String?   @db.Text
  budget_amount                Decimal?  @db.Decimal(14, 2)
  notes                        String?   @db.Text
  progress                     Int
  vendor                       String?   @db.VarChar(2048)
  document_link                String?   @db.VarChar(2048)
  is_recurring_initiative      Boolean   @default(false)
  recurring_period             String?   @db.VarChar(2048)
  occurance_limit              Int?      @db.UnsignedInt
  start_date                   DateTime? @db.Date
  end_date                     DateTime? @db.Date
  created_at                   DateTime? @db.Timestamp(0)
  updated_at                   DateTime? @db.Timestamp(0)
  deleted_at                   DateTime? @db.Timestamp(0)
  create_alert                 Boolean   @default(true)
  is_disable                   Boolean   @default(false)
  is_disable_note              String?
  level                        String?   @db.VarChar(20)
  process_maturity_level       String?
  practice_status              String?
  uuid_app_id                  String?   @db.Char(36)
  uuid_parent_sub_control_id   String?   @db.Char(36)
  uuid_sub_control_template_id String?   @db.Char(36)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model temp_task_reccur {
  Months                 Int?
  Date_of_First_Due_Date String? @map("Date of First Due Date") @db.Text
  Nmb                    Int?
  Task_id                Int     @default(0) @db.UnsignedInt
  uuid                   String? @unique(map: "temp_task_reccur_uuid_unique") @db.Char(36)
  uuid_Task_id           String? @db.Char(36)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model temp_tasks {
  task_id      Int     @db.UnsignedInt
  uuid         String? @unique(map: "temp_tasks_uuid_unique") @db.Char(36)
  uuid_task_id String? @db.Char(36)

  @@ignore
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model temp_user {
  id                   Int?
  uuid                 String? @unique(map: "temp_user_uuid_unique") @db.Char(36)
  organization_id      Int?
  partner_id           Int?
  uuid_organization_id String? @db.Char(36)
  uuid_partner_id      String? @db.Char(36)

  @@ignore
}

model template {
  id              String        @id @db.Char(36)
  entity_type     String
  entity_template Json
  organization_id Int?          @db.UnsignedInt
  partner_id      Int?          @db.UnsignedInt
  created_at      DateTime      @default(now()) @db.DateTime(0)
  updated_at      DateTime      @default(now()) @db.DateTime(0)
  deleted_at      DateTime?     @db.DateTime(0)
  recurrence      recurrence[]
  organization    organization? @relation(fields: [organization_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "template_ibfk_1")
  partner         partner?      @relation(fields: [partner_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "template_ibfk_2")

  @@index([organization_id], map: "organization_id")
  @@index([partner_id], map: "partner_id")
}

model user {
  id                                                              Int                        @id @default(autoincrement()) @db.UnsignedInt
  uuid                                                            String?                    @unique(map: "user_uuid_unique") @db.Char(36)
  first_name                                                      String
  last_name                                                       String
  email                                                           String
  password                                                        String
  department                                                      String?
  phone_number                                                    String?
  profile_img_path                                                String
  s3_bucket                                                       String                     @default("AWS_S3_BUCKET_STORAGE")
  s3_driver                                                       String                     @default("s3_storage")
  title                                                           String?
  role_id                                                         Int?                       @db.UnsignedInt
  is_accept_terms                                                 Boolean                    @default(false)
  created_at                                                      DateTime?                  @db.Timestamp(0)
  updated_at                                                      DateTime?                  @db.Timestamp(0)
  deleted_at                                                      DateTime?                  @db.Timestamp(0)
  type                                                            String                     @default("1")
  partner_id                                                      Int?                       @db.UnsignedInt
  organization_id                                                 Int?                       @db.UnsignedInt
  partner_role_id                                                 Int?                       @db.UnsignedInt
  color_code                                                      String?
  read_only                                                       Int                        @db.UnsignedInt
  is_demo_user                                                    Boolean                    @default(false)
  synced_in_auth0                                                 Boolean                    @default(false)
  email_not_synced_in_auth0                                       Boolean                    @default(false)
  auth0_adfs_id                                                   String?
  migrated_to_auth0                                               Boolean                    @default(false)
  landing_page                                                    String
  email_enabled                                                   Boolean                    @default(true)
  crm_id                                                          String?
  email_tasks                                                     Boolean                    @default(true)
  email_subcontrols                                               Boolean                    @default(true)
  email_comments                                                  Boolean                    @default(true)
  is_beta_user                                                    Boolean?
  notify                                                          String?                    @db.VarChar(255)
  roles                                                           Json?
  uuid_role_id                                                    String?                    @db.Char(36)
  uuid_partner_id                                                 String?                    @db.Char(36)
  uuid_organization_id                                            String?                    @db.Char(36)
  uuid_partner_role_id                                            String?                    @db.Char(36)
  uuid_auth0_adfs_id                                              String?                    @db.Char(36)
  uuid_crm_id                                                     String?                    @db.Char(36)
  auth0_id                                                        String?
  api_key                                                         api_key[]
  assessment                                                      assessment[]
  audit_activity_user_map                                         audit_activity_user_map[]
  audit_task_comment_users                                        audit_task_comment_users[]
  audit_users                                                     audit_users[]
  contact_request                                                 contact_request[]
  document_document_created_byTouser                              document[]                 @relation("document_created_byTouser")
  document_document_updated_byTouser                              document[]                 @relation("document_updated_byTouser")
  login_log                                                       login_log[]
  message_queue                                                   message_queue[]
  notification_schedules_notification_schedules_receiver_idTouser notification_schedules[]   @relation("notification_schedules_receiver_idTouser")
  notification_schedules_notification_schedules_sender_idTouser   notification_schedules[]   @relation("notification_schedules_sender_idTouser")
  organization_user_map                                           organization_user_map[]    @ignore
  permission_permission_created_byTouser                          permission[]               @relation("permission_created_byTouser")
  permission_permission_updated_byTouser                          permission[]               @relation("permission_updated_byTouser")
  reports                                                         reports[]
  sub_control_notes                                               sub_control_notes[]
  task_comments_read                                              task_comments_read[]
  task_history                                                    task_history[]
  organization                                                    organization?              @relation(fields: [organization_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "user_organization_id_foreign")
  role                                                            role?                      @relation(fields: [role_id], references: [id], onDelete: Restrict, onUpdate: Restrict, map: "user_role_id_foreign")
  user_org_program_map                                            user_org_program_map[]     @ignore
  vendor_form                                                     vendor_form[]

  @@index([organization_id], map: "user_organization_id_foreign")
  @@index([role_id], map: "user_role_id_foreign")
}

model user_activities {
  id               Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid             String?   @unique(map: "user_activities_uuid_unique") @db.Char(36)
  user_id          Int
  activity_id      Int
  meta             String?   @db.VarChar(5000)
  created_at       DateTime? @db.Timestamp(0)
  updated_at       DateTime? @db.Timestamp(0)
  json             Json?
  uuid_user_id     String?   @db.Char(36)
  uuid_activity_id String?   @db.Char(36)

  @@index([user_id, activity_id])
}

model user_connector_history {
  id                Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid              String?   @unique(map: "user_connector_history_uuid_unique") @db.Char(36)
  user_id           Int
  connector_id      Int
  response          String    @db.Text
  status            String
  created_at        DateTime? @db.Timestamp(0)
  updated_at        DateTime? @db.Timestamp(0)
  deleted_at        DateTime? @db.Timestamp(0)
  uuid_user_id      String?   @db.Char(36)
  uuid_connector_id String?   @db.Char(36)
}

model user_connector_responses {
  id                      Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid                    String?   @unique(map: "user_connector_responses_uuid_unique") @db.Char(36)
  user_connector_id       Int
  connector_field_id      Int
  value                   String?   @db.Text
  created_at              DateTime? @db.Timestamp(0)
  updated_at              DateTime? @db.Timestamp(0)
  deleted_at              DateTime? @db.Timestamp(0)
  uuid_user_connector_id  String?   @db.Char(36)
  uuid_connector_field_id String?   @db.Char(36)
}

model user_connectors {
  id                              Int                               @id @default(autoincrement()) @db.UnsignedInt
  uuid                            String?                           @unique(map: "user_connectors_uuid_unique") @db.Char(36)
  user_id                         Int
  organization_id                 Int
  connector_id                    Int
  created_at                      DateTime?                         @db.Timestamp(0)
  updated_at                      DateTime?                         @db.Timestamp(0)
  deleted_at                      DateTime?                         @db.Timestamp(0)
  guid                            String?
  uuid_user_id                    String?                           @db.Char(36)
  uuid_organization_id            String?                           @db.Char(36)
  uuid_connector_id               String?                           @db.Char(36)
  connector_subcontrols           connector_subcontrols[]
  task_connector_priority_mapping task_connector_priority_mapping[]
  task_connector_status_mapping   task_connector_status_mapping[]
}

model user_marketplace_solutions {
  id                          Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid                        String?   @unique(map: "user_marketplace_solutions_uuid_unique") @db.Char(36)
  framework_id                Int       @db.UnsignedInt
  user_id                     Int       @db.UnsignedInt
  solution_id                 Int       @db.UnsignedInt
  subcontrol_template_id      Int?
  category_id                 Int?
  status                      String?
  created_at                  DateTime? @db.Timestamp(0)
  updated_at                  DateTime? @db.Timestamp(0)
  deleted_at                  DateTime? @db.Timestamp(0)
  uuid_framework_id           String?   @db.Char(36)
  uuid_user_id                String?   @db.Char(36)
  uuid_solution_id            String?   @db.Char(36)
  uuid_subcontrol_template_id String?   @db.Char(36)
  uuid_category_id            String?   @db.Char(36)
}

model user_notifications {
  id                                     Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid                                   String?   @unique(map: "user_notifications_uuid_unique") @db.Char(36)
  user_id                                Int
  audit_id                               Int?
  organization_id                        Int?
  partner_generated_notification_id      Int?
  category                               String?
  subject                                String
  message                                String    @db.Text
  from                                   String
  read                                   Boolean   @default(false)
  display_option                         String?
  url                                    String?
  archived                               DateTime? @db.DateTime(0)
  end_date                               DateTime? @db.Date
  created_at                             DateTime? @db.Timestamp(0)
  updated_at                             DateTime? @db.Timestamp(0)
  read_at                                DateTime? @db.Timestamp(0)
  uuid_user_id                           String?   @db.Char(36)
  uuid_audit_id                          String?   @db.Char(36)
  uuid_organization_id                   String?   @db.Char(36)
  uuid_partner_generated_notification_id String?   @db.Char(36)

  @@index([user_id])
}

/// The underlying table does not contain a valid unique identifier and can therefore currently not be handled by Prisma Client.
model user_org_program_map {
  user_id                 Int          @db.UnsignedInt
  organization_id         Int          @db.UnsignedInt
  current_program_id      Int          @db.UnsignedInt
  level                   String?
  created_at              DateTime?    @db.Timestamp(0)
  updated_at              DateTime?    @db.Timestamp(0)
  uuid_user_id            String?      @db.Char(36)
  uuid_organization_id    String?      @db.Char(36)
  uuid_current_program_id String?      @db.Char(36)
  program                 program      @relation(fields: [current_program_id], references: [id], onUpdate: Restrict, map: "user_org_program_map_current_program_id_foreign")
  organization            organization @relation(fields: [organization_id], references: [id], onUpdate: Restrict, map: "user_org_program_map_organization_id_foreign")
  user                    user         @relation(fields: [user_id], references: [id], onUpdate: Restrict, map: "user_org_program_map_user_id_foreign")

  @@index([current_program_id], map: "user_org_program_map_current_program_id_foreign")
  @@index([organization_id], map: "user_org_program_map_organization_id_foreign")
  @@index([user_id], map: "user_org_program_map_user_id_foreign")
  @@ignore
}

model user_stats {
  id                           BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                         String?   @unique(map: "user_stats_uuid_unique") @db.Char(36)
  user_id                      Int
  last_login                   DateTime? @db.DateTime(0)
  num_of_logins                Int?
  num_of_logins_30_days        Int?
  num_of_subcontrols           Int?
  num_of_tasks                 Int?
  num_of_reports               Int?
  num_of_reports_30_days       Int?
  num_of_documents             Int?
  num_of_documents_30_days     Int?
  num_of_score_updates         Int?
  num_of_score_updates_30_days Int?
  created_at                   DateTime? @db.Timestamp(0)
  updated_at                   DateTime? @db.Timestamp(0)
  uuid_user_id                 String?   @db.Char(36)
}

model user_support_messages {
  id           Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid         String?   @unique(map: "user_support_messages_uuid_unique") @db.Char(36)
  user_id      Int?      @db.UnsignedInt
  type         String
  message      String    @db.Text
  created_at   DateTime? @db.Timestamp(0)
  updated_at   DateTime? @db.Timestamp(0)
  uuid_user_id String?   @db.Char(36)
}

model vendor_form {
  id                            BigInt                   @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                          String                   @db.Char(36)
  form_id                       BigInt                   @db.UnsignedBigInt
  vendor_id                     BigInt                   @db.UnsignedBigInt
  vendor_contact_id             BigInt                   @db.UnsignedBigInt
  current_form_instance_id      BigInt                   @db.UnsignedBigInt
  first_sent_at                 DateTime                 @db.DateTime(0)
  last_sent_at                  DateTime                 @db.DateTime(0)
  last_viewed_at                DateTime                 @db.DateTime(0)
  frequency                     vendor_form_frequency?
  created_at                    DateTime?                @db.Timestamp(0)
  updated_at                    DateTime?                @db.Timestamp(0)
  organization_id               BigInt                   @db.UnsignedBigInt
  due_date                      DateTime?                @db.Date
  sender_id                     Int?                     @db.UnsignedInt
  end_date                      DateTime?                @db.Date
  uuid_form_id                  String?                  @db.Char(36)
  uuid_vendor_id                String?                  @db.Char(36)
  uuid_vendor_contact_id        String?                  @db.Char(36)
  uuid_current_form_instance_id String?                  @db.Char(36)
  uuid_organization_id          String?                  @db.Char(36)
  uuid_sender_id                String?                  @db.Char(36)
  user                          user?                    @relation(fields: [sender_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "vendor_form_sender_id_foreign")
  vendor_form_occurrence        vendor_form_occurrence[]

  @@index([sender_id], map: "vendor_form_sender_id_foreign")
}

model vendor_form_contact_map {
  id                     BigInt  @id @default(autoincrement()) @db.UnsignedBigInt
  vendor_form_id         BigInt
  vendor_contact_id      BigInt
  uuid_vendor_form_id    String? @db.Char(36)
  uuid_vendor_contact_id String? @db.Char(36)
}

model vendor_form_occurrence {
  id                  Int         @id @default(autoincrement()) @db.UnsignedInt
  uuid                String?     @unique(map: "vendor_form_occurrence_uuid_unique") @db.Char(36)
  occurrence          Int         @db.UnsignedInt
  due_date            DateTime?   @db.DateTime(0)
  notified_at         DateTime?   @db.DateTime(0)
  notified            Boolean     @default(false)
  vendor_form_id      BigInt      @db.UnsignedBigInt
  created_at          DateTime?   @db.Timestamp(0)
  updated_at          DateTime?   @db.Timestamp(0)
  uuid_vendor_form_id String?     @db.Char(36)
  vendor_form         vendor_form @relation(fields: [vendor_form_id], references: [id], onDelete: Cascade, onUpdate: Restrict, map: "vendor_form_occurrence_vendor_form_id_foreign")

  @@index([vendor_form_id], map: "idx_vendor_form_id")
}

model vendor_question_documents {
  id                          BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                        String?   @unique(map: "vendor_question_documents_uuid_unique") @db.Char(36)
  document_id                 Int
  form_field_response_id      Int
  vendor_form_uuid            String    @db.Char(36)
  created_at                  DateTime? @db.Timestamp(0)
  updated_at                  DateTime? @db.Timestamp(0)
  uuid_document_id            String?   @db.Char(36)
  uuid_form_field_response_id String?   @db.Char(36)
}

model vendor_types {
  id              String        @id @db.Char(36)
  name            String
  partner_id      Int?          @db.UnsignedInt
  organization_id Int?          @db.UnsignedInt
  created_at      DateTime?     @db.Timestamp(0)
  updated_at      DateTime?     @db.Timestamp(0)
  deleted_at      DateTime?     @db.Timestamp(0)
  organization    organization? @relation(fields: [organization_id], references: [id], onDelete: Restrict, onUpdate: NoAction, map: "vendor_types_organization_id_foreign")
  partner         partner?      @relation(fields: [partner_id], references: [id], onDelete: Restrict, onUpdate: NoAction, map: "vendor_types_partner_id_foreign")

  @@index([organization_id], map: "vendor_types_organization_id_foreign")
  @@index([partner_id], map: "vendor_types_partner_id_foreign")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model vendors {
  id                   BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid                 String?   @unique(map: "vendors_uuid_unique") @db.Char(36)
  name                 String?
  organization_id      Int       @db.UnsignedInt
  created_at           DateTime? @db.Timestamp(0)
  updated_at           DateTime? @db.Timestamp(0)
  deleted_at           DateTime? @db.Timestamp(0)
  description          String    @db.Text
  street_1             String
  street_2             String
  city                 String
  state                String
  country              String
  zipcode              String
  note                 String    @db.Text
  status               Boolean
  risk_rating          Int?
  uuid_organization_id String?   @db.Char(36)
  type                 String?   @db.Text
  vendor_score         Float?
  manual_score         Boolean   @default(false)
}

model vendors_contact {
  id             BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid           String?   @unique(map: "vendors_contact_uuid_unique") @db.Char(36)
  vendor_id      Int
  first_name     String?
  last_name      String?
  email          String
  phone          String?
  is_primary     Boolean
  created_at     DateTime? @db.Timestamp(0)
  updated_at     DateTime? @db.Timestamp(0)
  deleted_at     DateTime? @db.Timestamp(0)
  uuid_vendor_id String?   @db.Char(36)
}

model vendors_documents {
  id               BigInt    @id @default(autoincrement()) @db.UnsignedBigInt
  uuid             String?   @unique(map: "vendors_documents_uuid_unique") @db.Char(36)
  vendor_id        Int
  document_id      Int
  user_id          BigInt?   @db.UnsignedBigInt
  created_at       DateTime? @db.Timestamp(0)
  updated_at       DateTime? @db.Timestamp(0)
  deleted_at       DateTime? @db.Timestamp(0)
  uuid_vendor_id   String?   @db.Char(36)
  uuid_document_id String?   @db.Char(36)
  uuid_user_id     String?   @db.Char(36)
}

model webhook {
  id              Int       @id @default(autoincrement()) @db.UnsignedInt
  uuid            String    @db.Char(36)
  url             String
  related_type    String
  related_id      Int       @db.UnsignedInt
  created_at      DateTime? @db.Timestamp(0)
  updated_at      DateTime? @db.Timestamp(0)
  uuid_related_id String?   @db.Char(36)
}

enum assessment_question_template_recommendations_audience {
  common_language
  general_business
  technical
  cybersecurity_professional
}

enum import_framework_staging_type {
  tasks
  risks
  subcontrols
}

enum assessment_question_template_recommendations_verbosity {
  brief
  adequate
  expansive
}

enum form_status {
  draft
  active
}

enum import_framework_staging_import_status {
  pending
  done
}

enum form_group_parent_type {
  form
  formInstance
}

enum form_instance_status {
  draft
  active
}

enum subcontrol_connector_evidence_mapping_update_frequency {
  daily
  weekly
  monthly
  quarterly
  yearly
}

enum organization_connector_update_frequency {
  daily
  weekly
  quarterly
  monthly
  yearly
}

enum control_activity_frequency {
  as_needed
  daily
  weekly
  monthly
  annually
}

enum form_field_type {
  yesNo
  short
  long
  radio
  date
  dropdown
  checkbox
  multipleCheckbox
  table
}

enum form_field_response_type {
  yesNo
  short
  long
  radio
  date
  dropdown
  checkbox
  multipleCheckbox
  table
}

enum vendor_form_frequency {
  daily
  weekly
  bi_weekly   @map("bi-weekly")
  monthly
  yearly
  bi_annually @map("bi-annually")
  bi_ennially @map("bi-ennially")
}

enum sub_control_verified {
  ZERO @map("0")
  ONE  @map("1")
}
